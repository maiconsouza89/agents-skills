---
"@mass-solutions/skills-cli": minor
---

`install`, `update` and `remove` append a JSON Lines audit entry per skill to `mass-skills.audit.jsonl` (or `~/.config/mass-skills/audit.jsonl` with `-g`), which `MASS_SKILLS_NO_AUDIT=1` turns off.

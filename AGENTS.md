# AGENTS.md

Regras para agentes de código que trabalham neste repositório: ver [@CLAUDE.md](CLAUDE.md).

As skills `mass-*` do catálogo já estão instaladas em `.agents/skills/` (e em `.claude/skills/` para o Claude Code), rastreadas em `mass-skills.lock.json`; `.claude/skills/` também abriga skills ainda em desenvolvimento. Ver "Dogfooding e skills em desenvolvimento" no `CLAUDE.md`.

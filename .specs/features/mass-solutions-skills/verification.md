# Mass Solutions Skills verification

**Verdict**: FAIL
**Profile**: standard
**Diff range**: 0fff8f7..89f3d3a
**Round**: 1 - full
**Verifier**: independent sub-agent (author != verifier)

Verificado em `89f3d3a` (branch `feature/implement-v1`). Árvore real limpa antes e depois (`git status --porcelain` vazio nos dois momentos); nenhum arquivo de código, teste ou spec foi editado além deste relatório. As duas renegociações do usuário (`b362a47` tema escuro, `89f3d3a` nome `maiconsouza89/agents-skills`) estão dentro do range e foram verificadas como estão em HEAD (C70, C82, C81, C6, C48).

Resumo: os 82 checks têm prova localizada e verde em HEAD; 30 conjuntos de Coverage recomputados; 5 falhas injetadas, 4 mortas e **1 sobrevivente** (tabela de agentes da door 10, cujo valor esperado nas provas de C45 e C68 é derivado do próprio código); 2 membros de Coverage sem prova; 3 linhas de Test policy não atendidas. O veredito é FAIL por essas linhas, não por nenhum teste vermelho.

## Binding sources

O perfil é `standard`; o plano marca `DESIGN.md` como binding **somente** sob o perfil `ui` (linha 6 das Sources do plano). Nenhuma fonte é binding neste perfil, então o passo 1 não roda.

| Source | Opened | Contradiction | Uncovered |
| --- | --- | --- | --- |
| nenhuma fonte binding sob `standard` (`DESIGN.md` só é binding sob `ui`) | n/a | none | - |

## Checks

Convenção da coluna `Proof run`: `lote 1`, `lote 2` e `lote 3` são as três invocações em batch descritas em `## Gate` (uma por alvo), todas com exit 0; `✓ <nome>` é o teste nomeado tal como apareceu individualmente na saída `--reporter=verbose`. Spikes e scripts da raiz foram rodados à parte, com o exit code registrado.

| Check | Claim | Proof run | Evidence | Result |
| --- | --- | --- | --- | --- |
| C1 | `pnpm install --frozen-lockfile` resolve `packages/core`, `packages/cli`, `apps/site` declarados em `pnpm-workspace.yaml` | `pnpm install --frozen-lockfile` exit 0 · lote 1 ✓ workspace declares the three packages and pnpm resolved them | `test/repo/workspace.test.ts:12` - `expect(ws.packages).toEqual(expect.arrayContaining(["packages/*", "apps/*"]))`; `:19` - `expect(existsSync(join(ROOT, "packages/cli/node_modules/@mass-solutions/skills-core/package.json"))).toBe(true)` | PASS |
| C2 | raiz expõe `check`, `validate`, `registry`, `build`, `test`, `scan`, `new-skill`, `stale`; `check` encadeia validate → registry --check → test | lote 1 ✓ root scripts expose check, validate, registry, build, test, scan, new-skill and stale | `test/repo/workspace.test.ts:25-26` - `expect(typeof pkg.scripts[s], s).toBe("string")` sobre os 8 nomes; `:33-34` - `expect(iRegistry).toBeGreaterThan(iValidate)` / `expect(iTest).toBeGreaterThan(iRegistry)` | PASS |
| C3 | `pnpm new-skill mass-exemplo` cria SKILL.md completo (fórmula, `version: "0.1.0"`, `reviewed` = hoje) + README de 3 linhas, e passa em `validateSkill` | lote 1 ✓ scaffolds a skill that validates | `test/repo/new-skill.test.ts:28` - `expect(res.status, res.stderr).toBe(0)`; `:33` - `toMatch(/\ndescription: .+\. Use when .+\. Do NOT use for .+\.\n/)`; `:36` - `toContain('\n  version: "0.1.0"\n')`; `:39` - `` toContain(`\n  reviewed: "${today}"\n`) ``; `:41` - `expect(readme).toHaveLength(3)`; `:42` - `expect(validateSkill(dir, { root })).toEqual([])` | PASS |
| C4 | nome fora de `^mass-[a-z0-9]+(-[a-z0-9]+)*$` → exit 2, imprime a regra, não cria arquivo | lote 1 ✓ rejects an invalid name with exit 2 and creates nothing | `test/repo/new-skill.test.ts:49` - `expect(res.status, bad).toBe(2)`; `:50` - `expect(res.stderr).toContain("^mass-[a-z0-9]+(-[a-z0-9]+)*$")`; `:51` - `expect(existsSync(join(root, "skills", bad))).toBe(false)` (4 nomes inválidos, `:47`) | PASS |
| C5 | `LICENSE` MIT, `skills/LICENSE` CC-BY-4.0, READMEs declaram a divisão | lote 1 ✓ licenses: MIT at the root, CC-BY-4.0 for skills, both declared in the readmes | `test/repo/docs.test.ts:17` - `expect(read("LICENSE")).toMatch(/^MIT License/)`; `:18` - `toContain("Creative Commons Attribution 4.0")`; `:21-23` - `toContain("MIT")`, `toContain("CC-BY-4.0")`, `toContain("skills/LICENSE")` nos dois READMEs | PASS |
| C6 | READMEs EN/PT com seções na ordem, três caminhos de instalação e aviso de hash | lote 1 ✓ readme sections in order for EN and PT, with the three install paths and the hash warning | `test/repo/docs.test.ts:29` / `:31` - `headingOrder(...)` (`:12` exige índice crescente); `:33-35` - `toContain("mass-skills")`, `toContain("npx skills add maiconsouza89/agents-skills")`, `toContain("/plugin marketplace add maiconsouza89/agents-skills")`; `:36` - `` toMatch(/Only the `mass-skills` CLI verifies hashes|Só o CLI `mass-skills` verifica hash/) `` | PASS |
| C7 | CONTRIBUTING: issue-first, fluxo new-skill → check → PR, conventional commits, regra de `metadata.reviewed` | lote 1 ✓ contributing: issue-first, the new-skill flow, conventional commits and the reviewed rule | `test/repo/docs.test.ts:44` - `toMatch(/[Ii]ssue primeiro|[Ii]ssue first/)`; `:45-46` - `toContain("pnpm new-skill")`, `toContain("pnpm check")`; `:48-49` - `toContain("Conventional Commits")`, `toContain("metadata.reviewed")`; `:50` - membros abrem PR direto | PASS |
| C8 | SECURITY: advisory privado, veta issue pública, validador + Snyk, allowlist com `expiresAt` | lote 1 ✓ security policy: private advisory, no public issue, validator + snyk, allowlist with expiresAt | `test/repo/docs.test.ts:55` - `toContain("/security/advisories/new")`; `:56` - `toMatch(/[Nn]unca abra uma issue pública|[Nn]ever open a public issue/)`; `:58-60` - `toContain("Snyk Agent Scan")`, `toContain("security-scan-allowlist.yaml")`, `toContain("expiresAt")` | PASS |
| C9 | CODEOWNERS exatamente `* @maiconsouza89` e `/skills/ @maiconsouza89` | lote 1 ✓ codeowners lists the maintainer for everything and for skills/ | `test/repo/github.test.ts:13` - `expect(lines).toEqual(["* @maiconsouza89", "/skills/ @maiconsouza89"])` | PASS |
| C10 | dois issue forms válidos com `name`, `description`, `body` e os campos do AC | lote 1 ✓ issue templates are valid issue forms with the required fields | `test/repo/github.test.ts:20-22` - `typeof form.name/description === "string"`, `Array.isArray(form.body)`; `:25` - `expect(ids(proposal)).toEqual(expect.arrayContaining(["name", "problem", "use-when", "do-not-use", "agents"]))`; `:26` - bug form ids; `:28` - `nameField.attributes.description` contém `mass-` | PASS |
| C11 | PR template com "Issue vinculada: #" e checklist `pnpm check` + `metadata.reviewed` | lote 1 ✓ pull request template requires a linked issue and the checklist | `test/repo/github.test.ts:33` - `toContain("Issue vinculada: #")`; `:34-35` - `toMatch(/- \[ \] .*pnpm check/)`, `toMatch(/- \[ \] .*metadata\.reviewed/)` | PASS |
| C12 | dependabot com `npm` e `github-actions`, ambos `weekly` | lote 1 ✓ dependabot covers npm and github-actions weekly | `test/repo/github.test.ts:42` - `expect(eco).toEqual(["github-actions", "npm"])`; `:43` - `expect(u.schedule.interval).toBe("weekly")` | PASS |
| C13 | AGENTS.md com regras do catálogo e bloco `## tlc-spec-lean` (`profile: standard`, `budget: 150k`); CLAUDE.md aponta AGENTS.md e mantém idioma | lote 1 ✓ agents and claude: catalog rules, the tlc-spec-lean block, and CLAUDE.md pointing to AGENTS.md | `test/repo/docs.test.ts:65-69` - `toContain("skills/<name>/SKILL.md")`, regex do nome, `"Use when"`, `"Do NOT use for"`, `"pnpm check"`; `:70` - `toMatch(/\n## tlc-spec-lean\n\nprofile: standard\nbudget: 150k\n/)`; `:72-74` - CLAUDE.md contém `AGENTS.md`, `pt-BR`, inglês | PASS |
| C14 | `.gitignore` cobre os 5 caminhos; `git ls-files` não retorna nada sob `.claude/skills/` | lote 1 ✓ tooling skills are untracked and the build outputs are ignored | `test/repo/gitignore.test.ts:15-16` - `expect(lines, entry).toContain(entry)` para `.claude/skills/`, `skills-lock.json`, `node_modules/`, `apps/site/dist/`, `.astro/`; `:18-19` - `git ls-files -- .claude/skills skills-lock.json` → `expect(tracked).toBe("")` | PASS |
| C15 | `validateCatalog` percorre só `skills/*`, ignora `.claude/`, `node_modules/`, `packages/`, `apps/`; `pnpm validate` sai com 0 | `pnpm validate` exit 0 (`validated 5 skill(s): 0 error(s), 0 warning(s)`) · lote 1 ✓ walks only the catalog and returns zero findings for a valid skill | `packages/core/test/validate.test.ts:22-23` - `expect(result.skills).toEqual(["mass-a"])` / `expect(result.findings).toEqual([])` com SKILL.md inválidos plantados em `.claude/skills/x`, `node_modules/y`, `packages/z`, `apps/w` (`:12-14`) | PASS |
| C16 | achado impresso como `<rule-id> <path>:<line> <mensagem>`, exit 1 | lote 1 ✓ prints one line per finding and exits 1 | `packages/core/test/validate-bin.test.ts:18` - `expect(res.status).toBe(1)`; `:22` - `expect(line).toMatch(/^[a-z-]+\/[a-z-]+ skills\/mass-bad\/SKILL\.md:\d+ .+$/)` em toda linha do stdout | PASS |
| C17 | chave fora das 6 → `frontmatter/unknown-key` | lote 1 ✓ unknown key › reports frontmatter/unknown-key for a key outside the spec (+ accepts every key of the spec) | `packages/core/test/rules.test.ts:26-27` - `f.filter((x) => x.rule === "frontmatter/unknown-key")` → `toHaveLength(1)`; `:37` - `not.toContain("frontmatter/unknown-key")` com as 6 chaves | PASS |
| C18 | `name` ≠ pasta, fora do regex, > 64 → `frontmatter/name` (3 casos) | lote 1 ✓ name rule (4 linhas `it.each` + accepts) | `packages/core/test/rules.test.ts:42-49` - `it.each([differs from the folder, does not match the mass- regex ×2, exceeds 64 characters])` → `expect(rules(f)).toContain("frontmatter/name")`; `:53` - aceita nome válido | PASS |
| C19 | `description` vazia, > 1024, fora da fórmula → `frontmatter/description` (3 casos) | lote 1 ✓ description rule (4 linhas + accepts) | `packages/core/test/rules.test.ts:58-65` - `it.each([is empty, exceeds 1024 characters, is off the formula, lacks the Do NOT clause])` → `toContain("frontmatter/description")`; `:69` - aceita a fórmula | PASS |
| C20 | `frontmatter/metadata` cobre os 9 casos | lote 1 ✓ metadata rule (11 linhas + absent + accepts) | `packages/core/test/rules.test.ts:74-94` - `it.each` com license≠CC-BY-4.0, author/version/category/tags/reviewed ausentes, version não semver, category desconhecida, reviewed futuro, valor não string (lista, número) → `toContain("frontmatter/metadata")`; `:103` - metadata ausente; `:98` - bloco válido aceito | PASS |
| C21 | `compatibility` > 500 → `frontmatter/compatibility` | lote 1 ✓ compatibility rule | `packages/core/test/rules.test.ts:110` - `toContain("frontmatter/compatibility")` com 501 chars; `:115` - `not.toContain(...)` com 500 | PASS |
| C22 | byte `0x00` nos primeiros 8192 → `content/binary` | lote 1 ✓ binary rule | `packages/core/test/rules.test.ts:122-124` - `hit = f.filter(rule === "content/binary")` → `toHaveLength(1)`, `hit[0].path === "skills/mass-x/assets/logo.png"`; `:129` - NUL após 8192 não é reportado | PASS |
| C23 | cada um dos 4 padrões de segredo → `security/secret` | lote 1 ✓ secret patterns (4 linhas) | `packages/core/test/rules.test.ts:134-143` - `it.each([aws access key, github token, private key, generic api key])` → `f.filter(rule === "security/secret")` `toHaveLength(1)`, `hit[0].line === 2` | PASS |
| C24 | cada um dos 5 padrões de shell (curl pipe sh, wget pipe bash, base64 -d pipe sh, `eval $(`, env pipe curl) → `security/shell` | lote 1 ✓ shell patterns (5 linhas + accepts) | `packages/core/test/rules.test.ts:148-158` - `it.each` com os 5 padrões → `toHaveLength(1)`; `:163` - curl sem pipe não é reportado | PASS |
| C25 | cada uma das 5 frases, sem caixa → `security/prompt-injection` | lote 1 ✓ prompt injection phrases (5 linhas) | `packages/core/test/rules.test.ts:168-176` - `it.each` com as 5 frases em caixa variada → `f.filter(rule === "security/prompt-injection")` `toHaveLength(1)` | PASS |
| C26 | script sem `#!` → `scripts/shebang`; sem `100755` no git → `scripts/executable` | lote 1 ✓ scripts rule (4 testes) | `packages/core/test/rules.test.ts:183` - `toContain("scripts/shebang")`; `:190` - `toContain("scripts/executable")` (modo fs); `:199` - `toContain("scripts/executable")` com `gitModes` `100644`; `:204-205` - aceita `#!` + bit | PASS |
| C27 | 3001 tokens → `size/tokens-warn` sem erro; 6001 tokens ou 501 linhas → `size/tokens` | lote 1 ✓ size rule (4 testes) | `packages/core/test/rules.test.ts:216-219` - `toHaveLength(1)`, `severity === "warn"`, `f.some(severity === "error") === false`; `:230-232` - 6001 tokens `size/tokens` error; `:239` - 501 linhas `size/tokens` error `toHaveLength(1)` | PASS |
| C28 | link relativo inexistente → `links/missing` | lote 1 ✓ missing link | `packages/core/test/rules.test.ts:246-249` - `toHaveLength(1)`, `message` contém `references/nope.md`; `:257` - links existentes/externos aceitos | PASS |
| C29 | `evals/` sem `triggers.json` com `should`/`shouldNot` → `evals/shape` | lote 1 ✓ evals shape (3 testes) | `packages/core/test/rules.test.ts:264` - `toContain("evals/shape")` sem triggers.json; `:269` - sem `shouldNot`; `:274` - aceita os dois arrays | PASS |
| C30 | `buildRegistry` na forma da door 3; `contentHash` bate com vetor conhecido | lote 1 ✓ registry shape follows door 3 · ✓ contentHash known answer | `packages/core/test/registry.test.ts:41` - `files.map(path)` `toEqual(["SKILL.md", "assets/t.md", "references/a.md", "references/z.md"])` (evals excluído, ordem por bytes); `:43` - `sha256` `/^[0-9a-f]{64}$/`; `:36` - tags array; `:49` - `tokens === Math.ceil(raw.length / 4)`; `:29` - deprecated copiado; `:28` - generatedAt ISO UTC; `:64` - `expect(contentHash).toBe(EXPECTED)` com vetor de `sha256sum` (`:54-56`) | PASS |
| C31 | `registry --check` exit 1 listando campos divergentes, ignorando `generatedAt` | lote 1 ✓ --check detects drift and lists the divergent fields | `packages/core/test/registry-bin.test.ts:25-28` - `expect(res.status).toBe(1)`, stdout contém `skills[0].files[0].sha256` e `skills[0].contentHash`, `not.toContain("generatedAt")` | PASS |
| C32 | deprecado com pasta, ou `replacedBy` inexistente → `deprecated/conflict` (2 casos) | lote 1 ✓ deprecated conflict (3 testes) | `packages/core/test/rules.test.ts:284-287` - `toHaveLength(1)`, `rule === "deprecated/conflict"`, `path === "skills/_deprecated.json"`; `:294-295` - replacedBy `mass-ghost`; `:301` - deprecação consistente `toEqual([])` | PASS |
| C33 | core exporta as 5 funções e tipos; cli/site não importam `gray-matter` nem `node:crypto` | lote 1 ✓ public api exposes the parse, validate, hash and registry functions · ✓ no duplicate parser | `packages/core/test/exports.test.ts:21` - `expect(typeof core[fn], fn).toBe("function")` para `parseSkill`, `validateSkill`, `validateCatalog`, `hashFiles`, `buildRegistry`; `:29-30` - `not.toMatch(/from\s+["']gray-matter["']/)`, `not.toMatch(/from\s+["'](node:)?crypto["']/)` sobre `packages/cli/src` e `apps/site/src` | PASS |
| C34 | 5 skills, cada SKILL.md ≤ 80 linhas, `validateCatalog` sem achados | lote 1 ✓ five example skills validate and stay under 80 lines | `test/repo/catalog.test.ts:14` - `expect(dirs).toEqual(FIVE)`; `:17` - `expect(lines, name).toBeLessThanOrEqual(80)`; `:20-21` - `result.skills` `toEqual(FIVE)`, `result.findings` `toEqual([])` | PASS |
| C35 | `mass-skill-authoring` com `references/` referenciado e `evals/triggers.json` ≥ 3/3 | lote 1 ✓ skill-authoring exercises references and evals | `test/repo/catalog.test.ts:27-29` - `refs.length >= 1`, `body.includes(`references/${f}`)`; `:31-32` - `triggers.should.length >= 3`, `shouldNot.length >= 3` | PASS |
| C36 | `mass-commit-message/scripts/` com shebang e executável; `mass-pr-description/assets/` referenciado | lote 1 ✓ commit-message scripts and pr-description assets | `test/repo/catalog.test.ts:40-41` - `startsWith("#!")`, `statSync(abs).mode & 0o111` `not.toBe(0)`; `:44-46` - `assets.length >= 1`, `body.includes(`assets/${a}`)` | PASS |
| C37 | `mass-code-review` com `metadata.requires: "git, gh"` e `allowed-tools`; `mass-security-checklist` só SKILL.md | lote 1 ✓ code-review requires and security-checklist minimal | `test/repo/catalog.test.ts:51` - `expect(...requires).toBe("git, gh")`; `:52-53` - `allowed-tools` string não vazia; `:55` - `expect(minimal).toEqual(["SKILL.md"])` | PASS |
| C38 | `_categories.json` cobre toda categoria usada; `_deprecated.json` é `{}` | lote 1 ✓ categories and deprecated files | `test/repo/catalog.test.ts:67` - `expect(ids, `${name} category`).toContain(meta.category)` para as 5; `:69` - `toEqual({})`; `:62-63` - `en`/`pt-br` strings | PASS |
| C39 | `marketplace.json` e `plugin.json` na forma da door 5 | lote 1 ✓ marketplace shape follows door 5 | `test/repo/marketplace.test.ts:11` - `mkt.name === "mass-solutions"`; `:15-18` - `plugin.name === "mass-solutions-skills"`, `source === "./"`, `skills` `toEqual(["./skills/"])`, `license === "CC-BY-4.0"`; `:23-25` - `manifest.name`, `version` igual ao plugin, `author.name === "Mass Solutions"` | PASS |
| C40 | `npx skills add ./ --list` lista exatamente as 5 `mass-*` | `bash tools/spike/npx-skills-list.sh` exit 0 (`PASS: npx skills lists exactly: mass-code-review mass-commit-message mass-pr-description mass-security-checklist mass-skill-authoring`) | `tools/spike/npx-skills-list.sh:12` - `grep -q "Found ${count} skill"` senão exit 1; `:15` - `[ "$listed" != "$expected" ]` → exit 1 (sobre export limpo da árvore, `:5`) | PASS |
| C41 | `claude plugin validate .` sai com 0 | `bash tools/spike/claude-plugin-validate.sh` exit 0 (`✔ Validation passed`) | `tools/spike/claude-plugin-validate.sh:3` - `set -euo pipefail`; `:5` - `claude plugin validate .` (exit ≠ 0 aborta o script) | PASS |
| C42 | marketplace add + plugin install deixam as 5 pastas; script desfaz ao final | `bash tools/spike/claude-plugin-install.sh` exit 0 (`installPath: ~/.claude/plugins/cache/mass-solutions/mass-solutions-skills/0.1.0`; `PASS: plugin installs exactly: ...`) | `tools/spike/claude-plugin-install.sh:19-24` - `claude plugin list --json` precisa conter `mass-solutions-skills@mass-solutions` senão exit 1; `:27` - `[ "$installed" != "$expected" ]` → exit 1; `:7-12` - cleanup uninstall + marketplace remove | PASS |
| C43 | `list` imprime `<name>  <version>  <category>  <description≤80>` ordenado por `name`, exit 0 (HTTP local) | lote 2 ✓ lists skills in name order with name, version, category and description up to 80 chars | `packages/cli/test/list.test.ts:13` - `expect(r.code).toBe(0)`; `:15` - `toEqual(["mass-alpha", "mass-beta", "mass-gamma"])`; `:17` - `` expect(lines[1]).toBe(`mass-beta  0.1.0  quality  ${beta.description.slice(0, 80)}`) ``; `:19` - descrição original > 80 | PASS |
| C44 | `search` filtra name/description/tags sem caixa; sem resultado `No skills match "<termo>"`, exit 0 | lote 2 ✓ search matches case-insensitively on name, description and tags · ✓ search with no match prints the message and exits 0 | `packages/cli/test/list.test.ts:24-25` - `BETA` → 1 linha `^mass-beta `; `:27` - `defects` por description; `:29` - `COMMITS` por tag → `["mass-alpha", "mass-gamma"]`; `:35-36` - `code === 0`, `expect(r.stdout).toBe('No skills match "zzz-nothing"\n')` | PASS |
| C45 | `install` grava em `<agent path>/<name>/` para os 8 agentes (projeto e `--global`) e registra no lockfile | lote 2 ✓ installs into every agent path, project and global, and records the lockfile entry | `packages/cli/test/install.test.ts:25` - `expect(cases).toHaveLength(8)`; `:29-33` - `code === 0`, `SKILL.md` idêntico ao do catálogo, `references/guide.md` e `scripts/run.sh` presentes em `join(p.cwd, c.project, "mass-alpha")`; `:35` - `lock.skills["mass-alpha"].agents` `toEqual([c.id])`; `:38-40` - `--global` grava em `join(p.home, c.global, ...)` e `~/.config/mass-skills/lock.json`. Nota: `c.project`/`c.global` vêm de `AGENTS` (`:24`), não da door 10 - ver F5 e Coverage | PASS |
| C46 | `sha256` ou `contentHash` divergente → `Integrity check failed for <name>: <path>`, exit 1, nada gravado (2 casos) | lote 2 ✓ integrity failure writes nothing: file sha256 mismatch · ✓ integrity failure writes nothing: contentHash mismatch with every file sha256 intact | `packages/cli/test/install.test.ts:65-67` - `code === 1`, `expect(r.stderr).toBe("Integrity check failed for mass-alpha: references/guide.md\n")`, `untouched(p)` (`:14-20`: cwd, `.claude/`, lockfile e home vazios); `:82-84` - `toBe("Integrity check failed for mass-alpha: contentHash\n")`, `untouched(p)` | PASS |
| C47 | falha de rede no 2º de 3 arquivos deixa agente e lockfile intactos | lote 2 ✓ partial download leaves nothing behind when the second of three files fails | `packages/cli/test/install.test.ts:96-99` - `code === 1`, stderr `^Failed to fetch http://127.0.0.1:\d+/main/skills/mass-alpha/references/guide.md: `, `fx.hits.get("skills/mass-alpha/SKILL.md") > 0` (1º arquivo já baixado), `untouched(p)` | PASS |
| C48 | agente desconhecido → mensagem com lista e fallback `npx skills add maiconsouza89/agents-skills`, exit 2 | lote 2 ✓ unsupported agent exits 2 naming the supported list and the npx fallback | `packages/cli/test/install.test.ts:108-111` - `code === 2`, `expect(r.stderr).toBe('Unsupported agent "vim". Supported: claude-code, cursor, codex, github-copilot, opencode, windsurf, gemini-cli, cline. For other agents use: npx skills add maiconsouza89/agents-skills\n')`, `untouched(p)` | PASS |
| C49 | `-a auto` escolhe por `.claude/`, `.agents/`, `.windsurf/`; sem nenhuma → exit 2 com ids | lote 2 ✓ auto detects agents from .claude, .agents and .windsurf folders in the cwd · ✓ auto with nothing detected exits 2 listing the supported ids | `packages/cli/test/install.test.ts:119-123` - `.claude/skills/mass-gamma/SKILL.md` e `.windsurf/skills/...` existem, `.agents` não, `agents` `toEqual(["claude-code", "windsurf"])`; `:128-135` - `.agents` → os 6 ids; `:141-142` - `code === 2`, stderr `^No agent detected in .*Supported: claude-code, ..., cline\n$` | PASS |
| C50 | deprecado → `"<old>" is deprecated since <since>: <reason>. Use: <replacedBy>`, exit 1; ausente → exit 2 sugerindo `search` | lote 2 ✓ deprecated is refused with the replacement and exit 1 · ✓ unknown skill exits 2 suggesting search | `packages/cli/test/install.test.ts:149-150` - `code === 1`, `toBe('"mass-old" is deprecated since 2026-01-01: renamed. Use: mass-alpha\n')`; `:157-158` - `code === 2`, `toBe('Unknown skill "mass-nope". Try: mass-skills search mass-nope\n')` | PASS |
| C51 | `..`, `/` inicial ou `name` fora do regex → `Unsafe path`, exit 1, nada gravado (3 casos) | lote 2 ✓ unsafe path is refused before anything is written: .., leading slash, bad name | `packages/cli/test/install.test.ts:164-168` - 3 casos (`../escape.md`, `/etc/passwd`, `Mass-Alpha`); `:177-179` - `expect(r.code, c.label).toBe(1)`, `toMatch(/^Unsafe path: /)`, nenhum hit além de `skills-registry.json`; `:180` - `untouched(p)` | PASS |
| C52 | lockfile na forma da door 4 via `.tmp` + rename, sem `.tmp` sobrando; não lê nem escreve `skills-lock.json` | lote 2 ✓ lockfile shape and atomic write: door 4 shape, tmp + rename, no tmp left, skills-lock.json untouched | `packages/cli/test/lockfile.test.ts:22-25` - `Object.keys(lock)` `toEqual(["version", "skills"])`, `version === 1`, chaves da entrada `["version", "contentHash", "ref", "agents", "installedAt"]`, hash hex; `:26` - `readdirSync(p.cwd).filter(endsWith(".tmp"))` `toEqual([])`; `:27-28` - `skills-lock.json` intacto e `grill-me` ausente do lock | PASS |
| C53 | `update`: modificado → `locally modified, skipped (use --force)`; registry maior → reinstala; igual → `up to date` | lote 2 ✓ update three outcomes: locally modified is skipped, newer registry version reinstalls, equal is up to date | `packages/cli/test/update.test.ts:31` - `expect(r.stdout).toBe("mass-alpha: locally modified, skipped (use --force)\nmass-beta: updated to 0.2.0\nmass-gamma: up to date\n")`; `:32` - edição local preservada; `:34-35` - lock alpha `0.1.0`, beta `0.2.0` | PASS |
| C54 | `update --force` sobrescreve e grava `installedAt` novo | lote 2 ✓ force overwrites local edits and records a new installedAt | `packages/cli/test/update.test.ts:52` - `toBe("mass-alpha: overwritten to 0.1.0\n")`; `:53` - conteúdo igual ao do catálogo; `:55` - `installedAt` `toBe("2026-09-14T12:00:00.000Z")` (antes `2020-01-01`, `:46`) | PASS |
| C55 | `update --check` só imprime estados e não escreve nada | lote 2 ✓ check writes nothing and prints every state | `packages/cli/test/update.test.ts:76` - `toBe("mass-alpha: locally modified\nmass-beta: update available 0.2.0\nmass-gamma: deprecated since 2026-02-02, use: mass-alpha\n")`; `:77-79` - lockfile, SKILL.md editado e mtime de beta inalterados; `:84` - `up to date` | PASS |
| C56 | `doctor` reporta `ok`, `missing`, `modified`, `deprecated`; exit 1 se algum ≠ ok | lote 2 ✓ doctor reports every state: ok, missing, modified, deprecated, and exits 1 when any is not ok | `packages/cli/test/doctor.test.ts:17-18` - `code === 0`, `toBe("mass-alpha: ok\nmass-beta: ok\nmass-gamma: ok\n")`; `:29-30` - `code === 1`, `toBe("mass-alpha: missing\nmass-beta: modified\nmass-gamma: deprecated\n")` | PASS |
| C57 | `remove` apaga só nos agentes registrados e remove a entrada, exit 0 | lote 2 ✓ removes only registered agents and drops the lockfile entry | `packages/cli/test/remove.test.ts:22-25` - `code === 0`, `.claude/skills/mass-alpha` removido, cópia manual em `.windsurf/skills/mass-alpha/SKILL.md` preservada, `mass-beta` intacta; `:27` - `Object.keys(lock.skills)` `toEqual(["mass-beta"])`; `:29` - remover de novo → exit 2 | PASS |
| C58 | registry/arquivo `404` ou erro de rede → `Failed to fetch <url>: <status ou erro>`, exit 1, nada alterado (2 casos) | lote 2 ✓ fetch failure: registry 404 and network error exit 1 with the url and change nothing | `packages/cli/test/install.test.ts:192-193` - `code === 1`, `toMatch(/^Failed to fetch http:\/\/127\.0\.0\.1:\d+\/main\/skills-registry\.json: 404\n$/)`, `untouched(p)`; `:201-203` - rede (`127.0.0.1:1`) `code === 1`, `Failed to fetch ...: <erro>` sem status numérico | PASS |
| C59 | exit 0/1/2 conforme door 11; erros só em `stderr` | lote 2 ✓ exit code contract: 0 success, 1 execution failure, 2 usage error, errors on stderr only | `packages/cli/test/exit-codes.test.ts:30` - `expect(r.code, row.argv.join(" ")).toBe(row.code)` sobre a tabela `:13-23` (3×0, 3×1, 5×2); `:31` - sucesso → `stderr === ""`; `:33-34` - falha → `stderr.length > 0`, `stdout === ""` | PASS |
| C60 | `mass-skills validate` e `registry --check` espelham os exit codes dos scripts da raiz | lote 2 ✓ validate and registry mirror root scripts: same exit codes on a valid and an invalid catalog | `packages/cli/test/mirrors.test.ts:24` - `expect(viaCli.code, root).toBe(core)` (catálogo bom e ruim); `:26-27` - `0` e `1`; `:30-37` - `registry --check` 1 (sem arquivo) / 0 (após gerar) / 1 (após drift) tanto em `registryMain` quanto via CLI | PASS |
| C82 | sem `MASS_SKILLS_BASE_URL`, base `https://raw.githubusercontent.com/maiconsouza89/agents-skills/<ref>/`, ref default `main` | lote 2 ✓ default base url is the raw github url of the catalog repo with ref main | `packages/cli/test/download.test.ts:6` - `expect(DEFAULT_BASE_URL).toBe("https://raw.githubusercontent.com/maiconsouza89/agents-skills/")`; `:7` - `baseUrl({}) === DEFAULT_BASE_URL`; `:8` - `registryUrl({}, "main")` = `.../main/skills-registry.json`; `:9-11` - `fileUrl` (assembly real: `packages/cli/src/download.ts:4,9-13`, `bin.ts:4-9` passa `process.env`) | PASS |
| C61 | build gera as rotas EN e sob `/pt-br/`, `search-index.json`, `404.html`; links internos começam com `/agents-skills/` | lote 3 ✓ generates every route under the base for en and pt-br (astro build real, `apps/site/test/helpers.ts:36`) · `pnpm build` raiz exit 0 (17 páginas) | `apps/site/test/build.test.ts:24-26` - `expect(files, e).toContain(e)` para `404.html`, `index.html`, `install/`, `agents/`, `search-index.json`, `pt-br/...`, `skills/<n>/` ×5 e `pt-br/skills/<n>/` ×5; `:27` - exatamente 5 sob `skills/`; `:31` - todo `href`/`src` absoluto `toMatch(^/agents-skills/)` (`helpers.ts:10` `BASE = "/agents-skills/"`); `:34` - índice com os 5 nomes | PASS |
| C62 | `content.config.ts` usa `glob()` com `base` no catálogo; sem cópia de SKILL.md em `apps/site/` | lote 3 ✓ reads the catalog in place through glob() and never copies SKILL.md into the site | `apps/site/test/build.test.ts:39-41` - `toContain("glob({")`, `toMatch(/pattern:\s*"\*\/SKILL\.md"/)`, `toMatch(/base:\s*CATALOG_ROOT/)`; `:43` - `astro.config.mjs` contém `process.env.MASS_CATALOG_ROOT ||= src("../../skills")`; `:45` - `expect(copies).toEqual([])` | PASS |
| C63 | home agrupa por categoria (rótulo do idioma), ordena por `name`, mostra name/description/version, busca, filtro e troca de idioma | lote 3 ✓ home lists grouped and ordered skills with search, filter and a language switch | `apps/site/test/build.test.ts:56` - seções `data-category` `toEqual(usedIds)` na ordem de `_categories.json`; `:61` - `h2` contém o rótulo `en`/`pt-br`; `:64` - `expect(itemNames).toEqual([...itemNames].sort())`; `:68-71` - href localizado, name, description, version; `:76` - `input#q`; `:78` - options `["", ...usedIds]`; `:79` - `[data-lang-switch]` href `toBe(switchTo)` | PASS |
| C64 | busca sem resultado mostra `No skills match "<termo>"` / `Nenhuma skill corresponde a "<termo>"` sem esconder o campo (jsdom com script real) | lote 3 ✓ no match message names the term and keeps the field visible | `apps/site/test/search.test.ts:45-46` - `expect(noMatch.hidden).toBe(false)`, `expect(noMatch.textContent).toBe(message)` (EN e PT, `:37-40`); `:47` - `input#q` `.hidden === false`; `:48` - todos `[data-skill]` ocultos; `:50-53` - busca com resultado volta a esconder a mensagem | PASS |
| C65 | página da skill com 3 partes da description, corpo EN nas duas rotas, metadata, files com bytes, contentHash, link fonte, painel com 3 comandos e aviso | lote 3 ✓ skill page content: description parts, body in english on both routes, metadata, files, hash, source and the install panel | `apps/site/test/build.test.ts:92-94` - `[data-what]`, `[data-when]`, `[data-not]` com texto exato; `:96` - `body.getAttribute("lang") === "en"`; `:114` - `expect(bodies[0]).toBe(bodies[1])`; `:100-102` - author/version/reviewed/tags, `[data-requires] === "git, gh"`, `[data-allowed-tools]`; `:104-106` - `[data-file]` = `reg.files` com `bytes`, `[data-content-hash] === reg.contentHash`; `:107` - `[data-source]` = `https://github.com/maiconsouza89/agents-skills/tree/main/skills/<name>`; `:109-112` - os 3 comandos e o aviso de hash | PASS |
| C66 | skill mínima omite seção de arquivos adicionais e lista só `SKILL.md` | lote 3 ✓ minimal skill omits extra files section and lists only SKILL.md; a skill with references/ shows it | `apps/site/test/build.test.ts:119` - `expect(minimal.querySelector("[data-extra-files]")).toBeNull()`; `:120` - `[data-file]` `toEqual(["SKILL.md"])`; `:122` - skill rica mostra `references/` | PASS |
| C67 | nome deprecado gera `/skills/<old>/` com `Deprecated since <since>`, `replacedBy` e sem painel (fixture via `MASS_CATALOG_ROOT`) | lote 3 ✓ deprecated page shows the banner, the replacement and no install panel | `apps/site/test/deprecated.test.ts:21-22` - `[data-deprecated]` contém `Deprecated since 2026-08-01` e o motivo; `:24-25` - link `mass-alpha` para `/agents-skills/[pt-br/]skills/mass-alpha/`; `:26-27` - `[data-install]` `toBeNull()`, `main` não contém `mass-skills install`; `:29` - página viva mantém o painel (assembly: `deprecated.test.ts:17` `MASS_CATALOG_ROOT`, `apps/site/src/lib/paths.ts:5-9`) | PASS |
| C68 | `/install/` com 3 caminhos em ordem e o que cada um verifica; `/agents/` com os 8 agentes, path de projeto e global | lote 3 ✓ install and agents pages: three paths in order with what each verifies, and the eight agents with both paths | `apps/site/test/build.test.ts:132` - títulos `toEqual(["1. mass-skills CLI", "2. npx skills add", "3. Claude Code marketplace"])` (PT `:128`); `:133-135` - `sha256`, `Nothing is verified`, `No hash verification`; `:140` - linhas `data-agent` `toEqual(AGENTS.map(id))`; `:143-144` - `` toContain(`${a.projectDir}/`) ``, `` toContain(`~/${a.globalDir}/`) ``. Nota: esperado vem de `AGENTS` (`:4`) - ver F5 | PASS |
| C69 | `/404.html` liga para `/` e `/pt-br/` | lote 3 ✓ 404 page links to both catalogs | `apps/site/test/build.test.ts:152-153` - `expect(hrefs).toContain("/agents-skills/")`, `toContain("/agents-skills/pt-br/")` | PASS |
| C70 | CSS: monoespaçada no `body`, `--color-canvas: #201d1d` fundo, `--color-ink: #fdfcfc` cor, `border-radius: 4px` só em interativos, sem `box-shadow`/`gradient`; home usa `[+]`/`[-]` | lote 3 ✓ design tokens: monospace everywhere, dark canvas, light ink, 4px radius only on interactive elements, no shadow or gradient, ascii markers | `apps/site/test/build.test.ts:158-159` - `toMatch(/--color-canvas:#201d1d/)`, `toMatch(/--color-ink:#fdfcfc/)`; `:160-162` - `body{...background:var(--color-canvas)`, `color:var(--color-ink)`, `font-family:var(--font-mono)`; `:163` - `--font-mono` termina em `monospace`; `:164-165` - `not.toMatch(/box-shadow/)`, `not.toMatch(/gradient/)`; `:170-171` - todo bloco com `border-radius` tem seletor `^(input|select|.button|.snippet|.badge)` e valor `4px`/`var(--radius-sm)`; `:174-175` - home contém `[+]` e `[-]` | PASS |
| C71 | nenhuma página além da home tem `<script`; a home tem exatamente um | lote 3 ✓ zero client js outside home, one script on the home page | `apps/site/test/build.test.ts:181-182` - `expect(count, f).toBe(1)` para `index.html`/`pt-br/index.html`, `toBe(0)` para todas as outras; `:184` - script referencia `search-index.json` | PASS |
| C72 | `ci.yml`: `pull_request` + `push` em `main`, `node-version-file: .nvmrc`, cache pnpm, install/check/build/`plugin validate`, sem `secrets.` | lote 1 ✓ ci workflow runs check, build and the marketplace validation on PRs and main without secrets | `test/repo/workflows.test.ts:26-27` - `Object.keys(wf.on).sort()` `toEqual(["pull_request", "push"])`, `push.branches` `toEqual(["main"])`; `:17-20` - `node-version-file === ".nvmrc"`, `cache === "pnpm"`, `pnpm/action-setup@`, `pnpm install --frozen-lockfile`; `:31-34` - `pnpm check`, `pnpm build`, `npx -y @anthropic-ai/claude-code@latest plugin validate .`, `not.toContain("secrets.")` | PASS |
| C73 | `pages.yml`: push em `main`, `pages: write` + `id-token: write`, `withastro/action` `path: apps/site`, `package-manager: pnpm@11`, node do `.nvmrc`, `actions/deploy-pages` | lote 1 ✓ pages workflow deploys the astro site from apps/site with the nvmrc node version | `test/repo/workflows.test.ts:39-41` - branches `["main"]`, `permissions.pages === "write"`, `id-token === "write"`; `:43-45` - `path === "apps/site"`, `package-manager === "pnpm@11"`, `node-version` contém `steps.node.outputs.version`; `:46` - passo `cat .nvmrc`; `:47-48` - `actions/deploy-pages@` com `needs: build` | PASS |
| C74 | `security-scan.yml`: push `main` + `pull_request`; Snyk condicionado a `push` ou PR do mesmo repo; `uvx snyk-agent-scan@latest skills --ci` com `SNYK_TOKEN` e flags da allowlist | lote 1 ✓ security scan workflow runs snyk only on push to main and same-repo PRs, with the allowlist flags | `test/repo/workflows.test.ts:53-54` - `push.branches` `["main"]`, `"pull_request" in wf.on`; `:61-63` - `run` contém `uvx snyk-agent-scan@latest skills --ci` e `steps.allowlist.outputs.flags`, `env.SNYK_TOKEN === "${{ secrets.SNYK_TOKEN }}"`; `:64-67` - `snyk.if` e `uv.if` contêm `github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository` | PASS |
| C75 | allowlist vencida → exit 1 `Allowlist entry expired: <risk> <skill> <expiresAt>`; vigentes → `--ignore-risks a,b`; passo sem condição no workflow | lote 1 ✓ expired entry fails with the entry named and no flags printed · ✓ valid entries become ignore flags, and an empty allowlist prints nothing (+ ✓ security scan workflow) | `test/repo/allowlist.test.ts:32-34` - `main(...)` `toBe(1)`, `stderr` `toBe("Allowlist entry expired: prompt-injection mass-alpha 2026-09-01\n")`, `stdout === ""`; `:52-53` - `toBe(0)`, `stdout` `toBe("--ignore-risks prompt-injection,dangerous-download\n")`; `:56-57` - vazia → nada; `test/repo/workflows.test.ts:58` - `expect(allow.if).toBeUndefined()` | PASS |
| C76 | `stale-skills.yml`: `cron: "0 9 * * 1"` + `workflow_dispatch`; cria ou atualiza issue `Stale skills` com label `stale-skill` a partir de `pnpm stale` | lote 1 ✓ stale skills workflow runs weekly and on demand, and creates or updates the Stale skills issue from pnpm stale | `test/repo/workflows.test.ts:72-73` - `schedule` `toEqual([{ cron: "0 9 * * 1" }])`, `"workflow_dispatch" in wf.on`; `:78` - `toMatch(/pnpm (--silent )?stale --days 90/)`; `:80-84` - script contém `const title = "Stale skills"`, `const label = "stale-skill"`, `issues.create`, `issues.update`, `labels: [label]` | PASS |
| C77 | saída vazia → não cria issue e fecha a aberta | lote 1 ✓ stale workflow closes when nothing is stale and creates nothing | `test/repo/workflows.test.ts:90` - `toMatch(/if \(!list\) \{\s*if \(existing\) await github\.rest\.issues\.update\([^)]*state: "closed"[^)]*\);\s*return;/)` | PASS |
| C78 | `pnpm stale --days 90` imprime `<name> - reviewed <date> (<n> days)` por vencida, nada quando não há; exit 0 | lote 1 ✓ lists stale skills older than --days with the reviewed date and the age · ✓ prints nothing when none is stale and still exits 0 | `test/repo/stale.test.ts:18-19` - `code === 0`, `expect(r.out).toBe("mass-old - reviewed 2026-06-15 (91 days)\n")` (limite de 90 dias exato excluído, `:15`); `:21` - `--days 10` lista 3; `:28-29` - `code === 0`, `out === ""` | PASS |
| C79 | `.changeset/config.json` com `baseBranch: "main"`, ignora `site`; pacotes `@mass-solutions/skills-core` e `@mass-solutions/skills-cli` | lote 1 ✓ changesets config targets main, ignores the site, and the two packages carry the scoped names | `test/repo/release.test.ts:12-13` - `config.baseBranch === "main"`, `config.ignore` `toEqual(["site"])`; `:14-15` - nomes dos pacotes; `:16` - `apps/site` chama-se `site` | PASS |
| C80 | `release.yml` em tags `v*`, roda `pnpm check`, `pnpm build`, `gh release create <tag> --generate-notes` | lote 1 ✓ release workflow runs on v* tags, checks, builds and creates a github release with generated notes | `test/repo/workflows.test.ts:95` - `wf.on.push.tags` `toEqual(["v*"])`; `:100-102` - `pnpm check`, `pnpm build`, `gh release create "${{ github.ref_name }}" --generate-notes` | PASS |
| C81 | registry commitado tem `version: 1` e `repo: "maiconsouza89/agents-skills"` | lote 1 ✓ committed registry version and repo | `packages/core/test/registry.test.ts:69-70` - `expect(reg.version).toBe(1)`, `expect(reg.repo).toBe("maiconsouza89/agents-skills")` sobre `skills-registry.json` da raiz | PASS |

Todos os 82 testes/scripts nomeados existem na árvore (`rg -n '^\s*it(\.each)?\(' ...`, um passe por arquivo, linhas citadas acima) e estão inteiros dentro do range `0fff8f7..89f3d3a` (`git diff --stat 0fff8f7..89f3d3a -- test packages/*/test apps/site/test tools/spike`: 36 arquivos, 2204 inserções, 0 remoções; `0fff8f7` só continha `CLAUDE.md`). Os testes marcados `↓ skipped` nos lotes (2 no lote 1, 4 no lote 2) são testes fora do filtro `-t`, não nomeados por nenhum check.

## Coverage

Recomputado a partir da autoridade de cada conjunto (código onde o conjunto é descoberto; plano onde o código tem de satisfazê-lo). `Unproven` lista o membro sem prova que o asserte.

| Set (size) | Recomputed from | Member -> proof | Unproven |
| --- | --- | --- | --- |
| validator rule ids (17, a linha do checks.md declara 16) | `rg -o '"[a-z]+/[a-z-]+"' packages/core/src` → 17 ids: `rules/frontmatter.ts:27,35,43-47,56-60,69-106,116-117`, `rules/content.ts:27,42-43,79,86,108,120`, `rules/size.ts:10,12,16`, `validate.ts:55,59` | `frontmatter/parse` rules.test.ts:19 (teste `frontmatter parse error`, rodado no lote 1, sem check id) · `frontmatter/unknown-key` C17 · `frontmatter/name` C18 · `frontmatter/description` C19 · `frontmatter/metadata` C20 · `frontmatter/compatibility` C21 · `content/binary` C22 · `security/secret` C23 · `security/shell` C24 · `security/prompt-injection` C25 · `scripts/shebang` C26 · `scripts/executable` C26 · `size/tokens-warn` C27 · `size/tokens` C27 · `links/missing` C28 · `evals/shape` C29 · `deprecated/conflict` C32 (os mesmos 17 aparecem em `rg -o` sobre `rules.test.ts` + `validate-bin.test.ts`) | - |
| secret patterns (4) | `packages/core/src/constants.ts:24-29` | C23, `rules.test.ts:134-138` table-driven sobre os 4 ids (`aws-access-key`, `github-token`, `private-key`, `generic-secret`) | - |
| shell patterns (5) | `constants.ts:31-37` | C24, `rules.test.ts:148-153` sobre os 5 | - |
| prompt-injection phrases (5) | `constants.ts:39-45` | C25, `rules.test.ts:168-173` sobre as 5, caixa variada | - |
| metadata cases (9) | AC 20 + `rules/frontmatter.ts:70-107` | C20, `rules.test.ts:74-84` (11 linhas: as 9 do AC + lista + número) e `:103` (metadata ausente) | - |
| name cases (3) | AC 18 + `rules/frontmatter.ts:45-47` | C18, `rules.test.ts:42-46` (4 linhas) | - |
| description cases (3) | AC 19 + `rules/frontmatter.ts:56-60` | C19, `rules.test.ts:58-62` (4 linhas) | - |
| size thresholds (3) | `constants.ts:19-21` (`TOKEN_WARN` 3000, `TOKEN_FAIL` 6000, `MAX_LINES` 500) | C27, `rules.test.ts:213-240` (3001 warn, 3000 sem warn, 6001 fail, 501 linhas fail) | - |
| CLI commands (8) | `packages/cli/src/run.ts:40-76` (`list`, `search`, `install`, `update`, `remove`, `doctor`, `validate`, `registry`) | `list` C43 · `search` C44 · `install` C45 · `update` C53 · `remove` C57 · `doctor` C56 · `validate` C60 · `registry` C60 | - |
| agent adapters (8) | ids: `packages/cli/src/agents.ts:14-23`; **paths: plano door 10** (o código é o que deve satisfazê-la; lido lado a lado, os 16 valores coincidem em HEAD) | ids: C48 `install.test.ts:110` lista literal dos 8 · cada id exercitado C45 `:24-40` · path de projeto literal: `claude-code` `.claude/skills` e `windsurf` `.windsurf/skills` C49 `:119-120` · `-a auto` por pasta C49 `:123,:128-135` | path global dos 8 agentes e path de projeto de `cursor`, `codex`, `github-copilot`, `opencode`, `gemini-cli`, `cline`: as únicas asserções (C45 `install.test.ts:24,30,39` e C68 `build.test.ts:4,143-144`) derivam o esperado de `AGENTS` (o módulo sob teste); nenhum teste escreve um valor da door 10 literalmente. Mutante F5 sobreviveu às duas provas |
| install refusals (5) | `installer.ts:20,23,28,30,44,50`, `agents.ts:50,57` | integrity C46 · unsupported agent C48 · deprecated C50 · unknown skill C50 · unsafe path C51 | - |
| unsafe path cases (3) | `paths.ts:4-14` | `..` C51 `:165` · leading `/` `:166` · bad name `:167` | - |
| update outcomes (4) | AC 53-54; `commands/update.ts:37-39,76-78` | `locally modified` C53 · `update available` C53 · `up to date` C53 · `--force` C54 (`update.ts:14-20` também tem `missing` e `not-in-registry` - ver Test policy) | - |
| doctor states (4) | `commands/doctor.ts:7,15,18-20` | `ok` C56 `:18` · `missing`/`modified`/`deprecated` C56 `:30` | - |
| CLI exit codes (3) | `packages/cli/src/types.ts:14-18` | `0` C59 `:13-15` · `1` C59 `:16-18` · `2` C59 `:19-23` | - |
| `GET raw/.../skills-registry.json` statuses (2) | plano Surface; `download.ts:32` | 200 C43 `list.test.ts:13` (HTTP local, `helpers.ts:52` `createServer`) · 404 C58 `install.test.ts:189-193` | - |
| `GET raw/.../skills/<name>/<path>` statuses (2) | plano Surface; `download.ts:32` | 200 C45 `install.test.ts:29-33` | 404 de arquivo: C58 e C47 usam destruição de socket para o arquivo (`helpers.ts:66-68`), não um 404; o mapeamento status → mensagem é o mesmo código (`download.ts:32`) provado só pela URL do registry (`:193`) |
| `mass-skills list`/`search` exits (3) | plano Surface; `run.ts:87-98` | 0 C43 `:13` · 1 C58 `install.test.ts:201` (`list` com rede fora) · 2 C59 `exit-codes.test.ts:23` (`list --bogus`) | - |
| `mass-skills install` exits (3) | plano Surface | 0 C45 `:29` · 1 C46 `:65` · 2 C48 `:108` | - |
| `mass-skills update`/`doctor` exits (3) | plano Surface | 0 C55 `update.test.ts:75` · 1 C56 `doctor.test.ts:29` | `2`: nenhum teste roda `update` ou `doctor` com erro de uso; a tabela de C59 (`exit-codes.test.ts:13-23`) só tem `list --bogus`, `frobnicate` e `install` sem args. O tratamento genérico do commander (`run.ts:92-95`) é o mesmo, mas o membro declarado não tem prova própria |
| site routes under `/agents-skills/` (8) | plano Surface; `apps/site/src/pages/**` (`404.astro`, `[...lang]/{index,install,agents}.astro`, `[...lang]/skills/[name].astro`, `search-index.json.ts`) | `/` C63 `:53` · `/pt-br/` C61 `:24` + C63 `:51` · `/skills/<name>/` C65 `:91` · `/pt-br/skills/<name>/` C61 `:25` + C65 `:89` · `/install/` C68 `:127` · `/agents/` C68 `:137` · `/search-index.json` C61 `:34` · `/404.html` C69 `:150` | - |
| site route statuses (2) | plano Surface | 200 C61 `:26` (arquivo gerado) · 404 C69 `:150-153` (`404.html` gerado; site estático, o status é do GitHub Pages) | - |
| `.claude-plugin/marketplace.json` readers (3) | plano Surface + door 5 | Claude Code validate C41 · Claude Code install C42 · `npx skills` C40 (os 3 scripts rodados aqui, exit 0) | - |
| workflows (5) | `ls .github/workflows` → `ci.yml`, `pages.yml`, `release.yml`, `security-scan.yml`, `stale-skills.yml` | `ci.yml` C72 · `pages.yml` C73 · `security-scan.yml` C74 · `stale-skills.yml` C76/C77 · `release.yml` C80 | - |
| root scripts (8) | `package.json:13-21` | presença dos 8: C2 `workspace.test.ts:25-26` · executados: `validate` (`pnpm validate` exit 0 aqui; bin via C16/C60) · `registry` C31/C60 (bin `registry.ts` via `pnpm exec tsx`) · `build` C61 (`astro build` direto; `pnpm build` da raiz rodado aqui, exit 0, `tsc` core/cli + 17 páginas) · `new-skill` C3/C4 (`pnpm exec tsx tools/new-skill.ts`) · `stale` C78 (`main()` in-process; o comando `pnpm --silent stale --days 90` asserido em C76 `:78`) · `check`/`test`: presença e encadeamento (C2 `:29-35`), não executados por prova (recursão) · `scan`: presença (C2) e wiring (C74 `:61`), não executado (exige `SNYK_TOKEN`, declarado no checks.md) | - |
| example skills (5) | `ls skills/` | `mass-skill-authoring` C35 · `mass-commit-message` C36 · `mass-pr-description` C36 · `mass-code-review` C37 · `mass-security-checklist` C37 · os 5 em C34 `:14` | - |
| one-way doors (11) | plano Landing | 1 C15 `validate.test.ts:22` · 2 C17-C21 · 3 C30 `registry.test.ts:41,47,64` · 4 C52 `lockfile.test.ts:22-26` · 5 C39 · 6 C32/C38 · 7 C1 `workspace.test.ts:12-17` · 8 C5 · 9 C82 `download.test.ts:6` + C61 `build.test.ts:31` (`base` `/agents-skills/`) + C48/C6 (`maiconsouza89/agents-skills`) · 11 C59 | 10 (tabela de agentes): ids provados; os paths literais da door não são asseridos por nenhuma prova (ver `agent adapters`) |
| entities (8) | plano Relations | `Skill` C30/C18 · `SkillFile` C30 `:41-45` · `Registry` C30/C81 · `Deprecation` C32/C50/C67 · `Lockfile` C52 · `LockEntry` C52 `:24` · `Agent` C45/C48 · `Category` C20/C38/C63; restrições: nome único = pasta C18, `Deprecation`/`Skill` disjuntos C32, `LockEntry` chaveada por nome C52 `:22-24`, `contentHash` determinístico C30 `:64` | - |
| startup config: download base (2 assemblies) | `packages/cli/src/download.ts:4` (`DEFAULT_BASE_URL`), `:9-13` (`baseUrl(env)`), `bin.ts:4-9` (`process.env`) | bin default C82 `download.test.ts:6-8` · harness via `MASS_SKILLS_BASE_URL` C43 (`helpers.ts:103` injeta a env; `install.test.ts:199` também) | - |
| startup config: catalog root for the site (2 assemblies) | `apps/site/astro.config.mjs:8` (`||= src("../../skills")`), `apps/site/src/lib/paths.ts:5-9` | default C62 `build.test.ts:43` + build real C61 · fixture via `MASS_CATALOG_ROOT` C67 `deprecated.test.ts:17` | - |
| `-a auto` markers (3) - conjunto sem linha no checks.md | `agents.ts:28-32` (`.claude`, `.agents`, `.windsurf`) | `.claude` e `.windsurf` C49 `:116-123` · `.agents` C49 `:125-135` | - |
| `update --check` states (4 no AC 55) - conjunto sem linha no checks.md | AC 55; `update.ts:42-57` | `locally modified`, `update available <v>`, `deprecated` C55 `:76` · `up to date` C55 `:84` (o código imprime ainda `missing` e `not in registry`, sem prova) | - |

Varredura por conjuntos sem linha: além dos dois acrescentados acima, todo conjunto nomeado em `Landing`, `Relations` e `Surface` do plano tem linha. As três linhas de prosa abaixo da tabela do checks.md conferem: os checks com status/rota (C43, C45, C58, C61, C69) atravessam a fronteira (servidor HTTP em `helpers.ts:52`, `astro build` em `apps/site/test/helpers.ts:36`); `scan` não é executado por nenhuma prova, como declarado.

## Test policy rows

Uma linha por (linha da política, arquivo classificado). Célula `Expectation met` só com `yes`/`no`; a razão está na coluna `Required proof`.

| Row | Files it classifies | Required proof | Expectation met |
| --- | --- | --- | --- |
| Decides, reached across a boundary | `packages/cli/src/commands/install.ts` + `installer.ts` (agentes, integridade, deprecada, path inseguro, staging) | fronteira: C45-C51, C58 via HTTP local e fs (`helpers.ts:52`); cada linha da tabela de decisão tem caso asserido: agente explícito/auto/desconhecido (C45, C49, C48), sha256 e contentHash (C46 `:65-67,:82-84`), deprecada/ausente (C50), 3 paths inseguros (C51), falha no meio (C47) | yes |
| Decides, reached across a boundary | `packages/cli/src/agents.ts` (tabela da door 10) | própria camada: o checks.md aponta "C45 table-driven", mas `install.test.ts:24` itera `AGENTS` e compara o resultado com `AGENTS` - a tabela nunca é confrontada com a door 10; mutante F5 (`agents.ts:16` `.cursor/skills` → `.cursorx/skills`) sobreviveu a C45 e a C68 | no |
| Decides, not reached across a boundary | `packages/core/src/rules/*.ts` (17 ids) | C17-C29, C32 na própria camada; cada id com pelo menos um caso positivo asserido (`rules.test.ts`, linhas na tabela de Checks) e os padrões table-driven cobrem todas as linhas de `constants.ts` | yes |
| Decides, not reached across a boundary | `packages/core/src/registry.ts` (ordenação, hash, merge de `deprecated`, diff) | C30 `registry.test.ts:41` ordem, `:47,:64` hash, `:29` deprecated; C31 `registry-bin.test.ts:25-28` diff com `generatedAt` ignorado | yes |
| Decides, not reached across a boundary | `packages/cli/src/commands/update.ts` (`classify`, 6 resultados em `update.ts:14-20`; `--force`; `--check`) | C53-C55 asserem `locally-modified`, `update-available`, `up-to-date`, `deprecated` (`update.test.ts:31,:76`), `--force` (`:52-55`) e `--check` (`:77-79`); as linhas `missing` (`update.ts:30`) e `not-in-registry` (`:34`) não têm caso asserido em nenhum teste | no |
| Decides, not reached across a boundary | `packages/cli/src/commands/doctor.ts` (4 estados, prioridade missing > modified > deprecated > ok) | C56 `doctor.test.ts:18,:30` asserem os 4 estados e o exit 1 | yes |
| Decides, not reached across a boundary | `tools/new-skill.ts` (nome inválido, pasta já existe, validação do scaffold) | C3 `new-skill.test.ts:28-42` e C4 `:49-51`; o ramo `skills/<name>/ already exists` (`new-skill.ts:17-19`, exit 2) não tem caso asserido | no |
| Decides, not reached across a boundary | `tools/stale.ts` (idade > dias; `reviewed` inválido ignorado) | C78 `stale.test.ts:18-21,:28-29` assere vencida, limite exato (90 dias não é vencida) e vazio; o desvio `reviewed` não parseável (`stale.ts:26`) é um guard de entrada sem caso, anotado como observação | yes |
| Decides, not reached across a boundary | `tools/allowlist.ts` (vencida, vigente, vazia, entrada malformada) | C75 `allowlist.test.ts:32-34,:52-53,:56-57,:61-62` cobre os 4 ramos; `expiresAt` malformada (`allowlist.ts:30`) é guard sem caso, observação | yes |
| Entry point that decides nothing | `apps/site/src/**` (templates; decisões só em agrupamento/ordenação e ramo deprecada) | na fronteira (saída do `astro build`): C61-C71; ordenação C63 `build.test.ts:64` (F4 mata), ramo deprecada C67 `deprecated.test.ts:21-27`, busca sem resultado C64 `search.test.ts:45-48` | yes |
| Entry point that decides nothing | `packages/core/src/bin/validate.ts`, `bin/registry.ts` | na fronteira (processo filho `pnpm exec tsx`): C16 `validate-bin.test.ts:18,:22`, C31 `registry-bin.test.ts:19-28`, C60 `mirrors.test.ts:24-37` (entrada aceita, cada rejeição, cada erro) | yes |
| Instrumentation, pass-throughs | `packages/cli/src/download.ts` (encaminha `fetch`, mapeia status → erro) | nenhuma prova própria exigida; coberto pelo consumidor: C58 `install.test.ts:192-193,:201-203`, C47 `:97`, C82 `download.test.ts:6-11` | yes |

## Faults injected

Isolamento: `git worktree add <scratch> HEAD` em `.../scratchpad/verify-wt` (HEAD `89f3d3a`), `pnpm install --frozen-lockfile --offline` ok; nunca `git stash`. Porcelain da árvore real: vazio antes, vazio depois; worktree removido com `git worktree remove --force` + `prune`; nenhum `.astro-build.lock` deixado. Cada falha foi aplicada sozinha (1 arquivo, 1 linha), a prova mais estreita rodada no scratch e o arquivo restaurado com `git checkout --` antes da seguinte (0 arquivos sujos após cada uma).

| Mutation | Location | Killed |
| --- | --- | --- |
| F1 - algoritmo do `contentHash`: separadores removidos (`"<path>\n<sha256>\n"` → `"<path><sha256>"`) | `packages/core/src/hash.ts:26` | yes |
| F2 - comparação do `contentHash` no install desligada (`if (false && contentHash !== skill.contentHash)`) | `packages/cli/src/installer.ts:50` | yes |
| F3 - decisão de três vias do `update`: `localHash !== entry.contentHash` → `===` (modificado vira "up to date" e vice-versa) | `packages/cli/src/commands/update.ts:37` | yes |
| F4 - ordenação da home dentro da categoria invertida (`a.name.localeCompare(b.name)` → `b.name.localeCompare(a.name)`) | `apps/site/src/lib/catalog.ts:27` | yes |
| F5 - tabela da door 10: path global de `cursor` `.cursor/skills` → `.cursorx/skills` | `packages/cli/src/agents.ts:16` | no |

Provas rodadas por falha (todas no scratch, `--reporter=verbose`):

- F1 - `pnpm vitest run packages/core/test/registry.test.ts -t "contentHash known answer"` → exit 1, `× contentHash known answer`, `Expected "3b79a4bf…" Received "d3c92a9f…"` (`registry.test.ts:64`).
- F2 - `pnpm vitest run packages/cli/test/install.test.ts -t "integrity failure writes nothing"` → exit 1, `× contentHash mismatch with every file sha256 intact` (`expected +0 to be 1`, `install.test.ts:82`); o caso `file sha256 mismatch` continua verde porque a checagem por arquivo (`installer.ts:44`) não foi tocada - as duas asserções cobrem superfícies distintas.
- F3 - `pnpm vitest run packages/cli/test/update.test.ts -t "update three outcomes"` → exit 1, `× update three outcomes`, stdout recebido começa em `mass-alpha: up to date\nmass-beta: lo…` (`update.test.ts:31`).
- F4 - `pnpm vitest run apps/site/test/build.test.ts -t "home lists grouped and ordered"` → exit 1, `× home lists grouped and ordered`, `expected ['mass-pr-description', …] to deeply equal ['mass-commit-message', …]` (`build.test.ts:64`; a categoria `workflow` tem 2 skills, então a ordem é observável).
- F5 - `pnpm vitest run packages/cli/test/install.test.ts -t "installs into every agent path"` → **exit 0, `✓ installs into every agent path, project and global, and records the lockfile entry`** (`install.test.ts:24` lê `AGENTS` mutado e instala em `~/.cursorx/skills/`, que é o que o teste então espera em `:39`). Segunda leitora da mesma tabela, `pnpm vitest run apps/site/test/build.test.ts -t "install and agents pages"` → **exit 0, `✓ install and agents pages`** (`build.test.ts:4,143-144` também derivam de `AGENTS`). O mutante sobrevive: um path errado na door 10 seria publicado no site e usado pelo `install --global` sem que nenhuma prova acuse.

A regra do validador sugerida como superfície não entrou no teto de 5: `rules.test.ts` assere o id exato de regra por linha table-driven (`:49,:65,:94,:142,:157,:176`), e F1 já exercita o core; a vaga foi usada pela tabela da door 10, cujas provas têm o valor esperado construído a partir do módulo sob teste.

## Swept existing rows

O `## Swept` do checks.md não tem nenhuma linha resolvida para `existing`: todas apontam para checks (C-ids) ou para `n/a` (authorization, métricas). A linha de `concurrency` declara uma limitação ("último vence, sem lock no v1"), não uma restrição existente no código; lida contra `lockfile.ts:34-41` (escrita por `.tmp` + `rename`, sem lock entre processos), a descrição é exata. Nada a contestar.

## Precision gaps and observations (not FAIL)

- **Conjunto de regras declarado como 16, recomputado 17**: `frontmatter/parse` (`rules/frontmatter.ts:25-28`) tem teste (`rules.test.ts:17-19`, verde no lote 1) e está no handoff de B1, mas nenhum check o nomeia e a linha de Coverage não foi atualizada. Gap de precisão do checks.md, não de código.
- **AC 61 nomeia `pnpm build`; C61 roda `astro build` direto** (`apps/site/test/helpers.ts:36`). O `pnpm build` da raiz (`tsc` em core e cli + site) foi rodado nesta verificação com exit 0 e 17 páginas; não há prova que o cubra.
- **C76/C77 provam o comportamento do workflow por substring/regex sobre o fonte do `github-script`** (`workflows.test.ts:80-84,:90`); o YAML não roda fora do GitHub. Aceitável para o perfil, mas é asserção de texto, não de comportamento.
- **404 de rota no site é provado como existência de `dist/404.html`** (C69), não como status HTTP; a Surface do plano descreve o serviço do GitHub Pages.
- **`stale.ts:26` e `allowlist.ts:30`**: guards de entrada (`reviewed` não parseável, `expiresAt` malformada) sem caso asserido; não são linhas da tabela de decisão dos ACs, registrados como observação.
- Sem `-a`, o `install` usa `auto` por default (`run.ts:51`), como o handoff de B2 declarou; C49 `:126` cobre.

## Gate

Comandos rodados na raiz do repositório real em `89f3d3a`, todos com exit 0:

- `pnpm install --frozen-lockfile` - exit 0 (C1)
- lote 1: `pnpm vitest run test/repo/{workspace,new-skill,docs,github,gitignore,catalog,marketplace,workflows,allowlist,stale,release}.test.ts packages/core/test/{validate,validate-bin,rules,registry,registry-bin,exports}.test.ts -t "<alternação dos 54 nomes de C1-C39, C72-C81 + frontmatter parse error>" --reporter=verbose` - 17 arquivos, **100 passed, 0 failed** (2 skipped fora do filtro)
- lote 2: `pnpm vitest run packages/cli/test/{list,install,lockfile,update,doctor,remove,exit-codes,mirrors,download}.test.ts -t "<alternação dos 22 nomes de C43-C60, C82>" --reporter=verbose` - 9 arquivos, **23 passed, 0 failed** (4 skipped fora do filtro)
- lote 3: `pnpm vitest run apps/site/test/{build,search,deprecated}.test.ts -t "<alternação dos 11 nomes de C61-C71>" --reporter=verbose` - 3 arquivos, **11 passed, 0 failed** (astro build real, 10 s)
- `pnpm validate` - exit 0, `validated 5 skill(s): 0 error(s), 0 warning(s)` (C15)
- `bash tools/spike/npx-skills-list.sh` - exit 0 (C40) · `bash tools/spike/claude-plugin-validate.sh` - exit 0 (C41) · `bash tools/spike/claude-plugin-install.sh` - exit 0, plugin instalado e desinstalado (C42)
- `pnpm build` - exit 0 (evidência extra para AC 61)
- Fault injection no scratch: 5 mutantes, 4 mortos, 1 sobrevivente (F5)

`python3 .claude/skills/tlc-spec-lean/scripts/validate_verification.py mass-solutions-skills` - saída registrada abaixo após a escrita deste relatório.

**Ranked gaps**:

1. Mutante sobrevivente F5 - tabela da door 10 - `packages/cli/src/agents.ts:16`; provas C45 (`packages/cli/test/install.test.ts:24`) e C68 (`apps/site/test/build.test.ts:4`) derivam o valor esperado de `AGENTS`. Correção: asserir os 8 pares de paths literalmente (contra a door 10) num teste da própria camada.
2. Coverage `mass-skills update/doctor exits (3)` - exit `2` sem prova - `packages/cli/test/exit-codes.test.ts:13-23` (no evidence para `update`/`doctor`).
3. Test policy - `packages/cli/src/commands/update.ts:30,34` (`missing`, `not-in-registry`) e `tools/new-skill.ts:17-19` (`already exists`) sem caso asserido.
4. Coverage `GET raw/.../skills/<name>/<path> statuses` - 404 de arquivo sem prova direta - `packages/cli/test/install.test.ts:93` + `helpers.ts:66-68` (socket destruído, não 404).
5. Precisão - linha `validator rule ids (16)` do checks.md está desatualizada (17 ids, `frontmatter/parse` sem check id).

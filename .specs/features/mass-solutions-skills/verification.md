# Mass Solutions Skills verification

**Verdict**: FAIL
**Profile**: standard
**Diff range**: 0fff8f7..75e4b94
**Round**: 3 - scoped
**Verifier**: independent sub-agent (author != verifier)

Rodada 3, escopada pelo diff do fix `123d165..75e4b94` (`git diff --stat`: 5 arquivos - `packages/core/test/rules.test.ts` (+15 linhas: o bloco `it.each` de C86, `:22-35`), `checks.md` (claim de C86 reescrito e a linha `frontmatter parse causes (4)` acrescentada à Coverage), o relatório da rodada 2 e os dois arquivos de lessons; nenhum arquivo de produção) e pelo único veredito não-PASS da rodada 2 (C86 e os membros `Unproven` de `frontmatter parse causes`). Verificado em `75e4b94` (branch `feature/implement-v1`). Árvore real limpa antes e depois (`git status --porcelain` vazio nos dois momentos); nenhum arquivo além deste relatório foi editado. Cada seção diz de onde veio: `verified at 75e4b94` foi refeito nesta rodada; `carried from 123d165` foi herdado da rodada 2 e `carried from 89f3d3a` da rodada 1 (onde a rodada 2 já herdava), porque o fix não podia tê-los tocado.

Resumo: as 86 provas rodaram em full no novo HEAD e estão verdes (140 testes nomeados, 3 lotes + `pnpm validate` + 3 spikes); 4 faltas novas injetadas no parser (`packages/core/src/frontmatter.ts`), 3 mortas, **1 sobrevivente**. O fix fechou a lacuna literal da rodada 2 - `unterminated frontmatter`, `invalid YAML` e `frontmatter is not a mapping` agora têm um caso cada no `it.each` de `rules.test.ts:23-35`, com `toHaveLength(1)` e `message toContain(<causa>)` - mas reescreveu o claim de C86 para "cada uma das 4 causas gera **exatamente um** `frontmatter/parse` **cuja mensagem nomeia a causa**" e deixou a quarta causa, `missing frontmatter`, no teste pré-fix (`rules.test.ts:17-20`), que assere só `toContain("frontmatter/parse")`. O mutante Fh (`missing` rotulado `unterminated frontmatter`, `frontmatter.ts:25`) passa nos 4 testes de C86 e na suíte inteira do core + repo (106 passed). O veredito é FAIL por esse mutante sobrevivente, pelo `Result` FAIL de C86 e pelo membro `missing frontmatter` sem prova na força do claim; a lacuna é de uma asserção e o fix é uma linha (ver `Ranked gaps`).

## Binding sources

carried from 89f3d3a - o perfil é `standard`; o plano marca `DESIGN.md` como binding somente sob `ui` (Sources, linha 6). Nenhuma fonte binding neste perfil; o passo 1 não roda e o fix não tocou a interface.

| Source | Opened | Contradiction | Uncovered |
| --- | --- | --- | --- |
| nenhuma fonte binding sob `standard` (`DESIGN.md` só é binding sob `ui`) | n/a | none | - |

## Checks

verified at 75e4b94 (coluna `Proof run` inteira: todas as provas re-rodaram nesta rodada). Convenção: `lote 1`, `lote 2` e `lote 3` são as três invocações em batch descritas em `## Gate`, todas com exit 0 no HEAD `75e4b94`; `✓ <nome>` é o teste nomeado tal como apareceu individualmente na saída `--reporter=verbose`. Na coluna `Evidence`, `verified at 75e4b94` marca citações refeitas porque o fix tocou o arquivo de teste: `packages/core/test/rules.test.ts` ganhou 15 linhas em `:22-35`, deslocando em +15 tudo a partir daí, logo C17-C29 e C32 foram re-citadas com `rg -n` no HEAD e conferidas uma a uma; `carried from 123d165` e `carried from 89f3d3a` marcam citações herdadas de arquivos que o diff do fix não tocou (`git diff 123d165..75e4b94 --stat` não os lista, logo as linhas não se moveram).

| Check | Claim | Proof run | Evidence | Result |
| --- | --- | --- | --- | --- |
| C1 | `pnpm install --frozen-lockfile` resolve `packages/core`, `packages/cli`, `apps/site` declarados em `pnpm-workspace.yaml` | `pnpm install --frozen-lockfile` exit 0 · lote 1 ✓ workspace declares the three packages and pnpm resolved them | carried from 89f3d3a: `test/repo/workspace.test.ts:12` - `expect(ws.packages).toEqual(expect.arrayContaining(["packages/*", "apps/*"]))`; `:19` - `expect(existsSync(join(ROOT, "packages/cli/node_modules/@mass-solutions/skills-core/package.json"))).toBe(true)` | PASS |
| C2 | raiz expõe `check`, `validate`, `registry`, `build`, `test`, `scan`, `new-skill`, `stale`; `check` encadeia validate → registry --check → test | lote 1 ✓ root scripts expose check, validate, registry, build, test, scan, new-skill and stale | carried from 89f3d3a: `test/repo/workspace.test.ts:25-26` - `expect(typeof pkg.scripts[s], s).toBe("string")` sobre os 8 nomes; `:33-34` - `expect(iRegistry).toBeGreaterThan(iValidate)` / `expect(iTest).toBeGreaterThan(iRegistry)` | PASS |
| C3 | `pnpm new-skill mass-exemplo` cria SKILL.md completo (fórmula, `version: "0.1.0"`, `reviewed` = hoje) + README de 3 linhas, e passa em `validateSkill` | lote 1 ✓ scaffolds a skill that validates | carried from 123d165: `test/repo/new-skill.test.ts:28` - `expect(res.status, res.stderr).toBe(0)`; `:33` - `toMatch(/\ndescription: .+\. Use when .+\. Do NOT use for .+\.\n/)`; `:36` - `toContain('\n  version: "0.1.0"\n')`; `:39` - `` toContain(`\n  reviewed: "${today}"\n`) ``; `:41` - `expect(readme).toHaveLength(3)`; `:42` - `expect(validateSkill(dir, { root })).toEqual([])` | PASS |
| C4 | nome fora de `^mass-[a-z0-9]+(-[a-z0-9]+)*$` → exit 2, imprime a regra, não cria arquivo | lote 1 ✓ rejects an invalid name with exit 2 and creates nothing | carried from 123d165: `test/repo/new-skill.test.ts:60` - `expect(res.status, bad).toBe(2)`; `:61` - `expect(res.stderr).toContain("^mass-[a-z0-9]+(-[a-z0-9]+)*$")`; `:62` - `expect(existsSync(join(root, "skills", bad))).toBe(false)` (4 nomes inválidos, `:58`) | PASS |
| C85 | `pnpm new-skill` sobre skill existente → exit 2, imprime `skills/<name>/ already exists`, não altera o SKILL.md | lote 1 ✓ refuses an existing skill with exit 2 and leaves it untouched | carried from 123d165: `test/repo/new-skill.test.ts:47` - `expect(run(["mass-exemplo", "--root", root]).status).toBe(0)` (precondição: scaffold prévio); `:51` - `expect(res.status).toBe(2)`; `:52` - `expect(res.stderr).toContain("skills/mass-exemplo/ already exists")`; `:53` - `expect(readFileSync(skillPath, "utf8")).toBe("edited by hand\n")` (guard em `tools/new-skill.ts:17-20`; mutante Fc morto) | PASS |
| C5 | `LICENSE` MIT, `skills/LICENSE` CC-BY-4.0, READMEs declaram a divisão | lote 1 ✓ licenses: MIT at the root, CC-BY-4.0 for skills, both declared in the readmes | carried from 89f3d3a: `test/repo/docs.test.ts:17` - `expect(read("LICENSE")).toMatch(/^MIT License/)`; `:18` - `toContain("Creative Commons Attribution 4.0")`; `:21-23` - `toContain("MIT")`, `toContain("CC-BY-4.0")`, `toContain("skills/LICENSE")` nos dois READMEs | PASS |
| C6 | READMEs EN/PT com seções na ordem, três caminhos de instalação e aviso de hash | lote 1 ✓ readme sections in order for EN and PT, with the three install paths and the hash warning | carried from 89f3d3a: `test/repo/docs.test.ts:29` / `:31` - `headingOrder(...)` (`:12` exige índice crescente); `:33-35` - `toContain("mass-skills")`, `toContain("npx skills add maiconsouza89/agents-skills")`, `toContain("/plugin marketplace add maiconsouza89/agents-skills")`; `:36` - `` toMatch(/Only the `mass-skills` CLI verifies hashes|Só o CLI `mass-skills` verifica hash/) `` | PASS |
| C7 | CONTRIBUTING: issue-first, fluxo new-skill → check → PR, conventional commits, regra de `metadata.reviewed` | lote 1 ✓ contributing: issue-first, the new-skill flow, conventional commits and the reviewed rule | carried from 89f3d3a: `test/repo/docs.test.ts:44` - `toMatch(/[Ii]ssue primeiro|[Ii]ssue first/)`; `:45-46` - `toContain("pnpm new-skill")`, `toContain("pnpm check")`; `:48-49` - `toContain("Conventional Commits")`, `toContain("metadata.reviewed")`; `:50` - membros abrem PR direto | PASS |
| C8 | SECURITY: advisory privado, veta issue pública, validador + Snyk, allowlist com `expiresAt` | lote 1 ✓ security policy: private advisory, no public issue, validator + snyk, allowlist with expiresAt | carried from 89f3d3a: `test/repo/docs.test.ts:55` - `toContain("/security/advisories/new")`; `:56` - `toMatch(/[Nn]unca abra uma issue pública|[Nn]ever open a public issue/)`; `:58-60` - `toContain("Snyk Agent Scan")`, `toContain("security-scan-allowlist.yaml")`, `toContain("expiresAt")` | PASS |
| C9 | CODEOWNERS exatamente `* @maiconsouza89` e `/skills/ @maiconsouza89` | lote 1 ✓ codeowners lists the maintainer for everything and for skills/ | carried from 89f3d3a: `test/repo/github.test.ts:13` - `expect(lines).toEqual(["* @maiconsouza89", "/skills/ @maiconsouza89"])` | PASS |
| C10 | dois issue forms válidos com `name`, `description`, `body` e os campos do AC | lote 1 ✓ issue templates are valid issue forms with the required fields | carried from 89f3d3a: `test/repo/github.test.ts:20-22` - `typeof form.name/description === "string"`, `Array.isArray(form.body)`; `:25` - `expect(ids(proposal)).toEqual(expect.arrayContaining(["name", "problem", "use-when", "do-not-use", "agents"]))`; `:26` - bug form ids; `:28` - `nameField.attributes.description` contém `mass-` | PASS |
| C11 | PR template com "Issue vinculada: #" e checklist `pnpm check` + `metadata.reviewed` | lote 1 ✓ pull request template requires a linked issue and the checklist | carried from 89f3d3a: `test/repo/github.test.ts:33` - `toContain("Issue vinculada: #")`; `:34-35` - `toMatch(/- \[ \] .*pnpm check/)`, `toMatch(/- \[ \] .*metadata\.reviewed/)` | PASS |
| C12 | dependabot com `npm` e `github-actions`, ambos `weekly` | lote 1 ✓ dependabot covers npm and github-actions weekly | carried from 89f3d3a: `test/repo/github.test.ts:42` - `expect(eco).toEqual(["github-actions", "npm"])`; `:43` - `expect(u.schedule.interval).toBe("weekly")` | PASS |
| C13 | AGENTS.md com regras do catálogo e bloco `## tlc-spec-lean` (`profile: standard`, `budget: 150k`); CLAUDE.md aponta AGENTS.md e mantém idioma | lote 1 ✓ agents and claude: catalog rules, the tlc-spec-lean block, and CLAUDE.md pointing to AGENTS.md | carried from 89f3d3a: `test/repo/docs.test.ts:65-69` - `toContain("skills/<name>/SKILL.md")`, regex do nome, `"Use when"`, `"Do NOT use for"`, `"pnpm check"`; `:70` - `toMatch(/\n## tlc-spec-lean\n\nprofile: standard\nbudget: 150k\n/)`; `:72-74` - CLAUDE.md contém `AGENTS.md`, `pt-BR`, inglês | PASS |
| C14 | `.gitignore` cobre os 5 caminhos; `git ls-files` não retorna nada sob `.claude/skills/` | lote 1 ✓ tooling skills are untracked and the build outputs are ignored | carried from 89f3d3a: `test/repo/gitignore.test.ts:15-16` - `expect(lines, entry).toContain(entry)` para `.claude/skills/`, `skills-lock.json`, `node_modules/`, `apps/site/dist/`, `.astro/`; `:18-19` - `git ls-files -- .claude/skills skills-lock.json` → `expect(tracked).toBe("")` | PASS |
| C15 | `validateCatalog` percorre só `skills/*`, ignora `.claude/`, `node_modules/`, `packages/`, `apps/`; `pnpm validate` sai com 0 | `pnpm validate` exit 0 (`validated 5 skill(s): 0 error(s), 0 warning(s)`) · lote 1 ✓ walks only the catalog and returns zero findings for a valid skill | carried from 89f3d3a: `packages/core/test/validate.test.ts:22-23` - `expect(result.skills).toEqual(["mass-a"])` / `expect(result.findings).toEqual([])` com SKILL.md inválidos plantados em `.claude/skills/x`, `node_modules/y`, `packages/z`, `apps/w` (`:12-14`) | PASS |
| C16 | achado impresso como `<rule-id> <path>:<line> <mensagem>`, exit 1 | lote 1 ✓ prints one line per finding and exits 1 | carried from 89f3d3a: `packages/core/test/validate-bin.test.ts:18` - `expect(res.status).toBe(1)`; `:22` - `expect(line).toMatch(/^[a-z-]+\/[a-z-]+ skills\/mass-bad\/SKILL\.md:\d+ .+$/)` em toda linha do stdout | PASS |
| C86 | cada uma das 4 causas do parser (ausente, não terminado, YAML inválido, não-mapa) gera **exatamente um** `frontmatter/parse` **cuja mensagem nomeia a causa** (17º rule id) | lote 1 ✓ frontmatter parse error › reports frontmatter/parse when the fences are missing · ✓ reports frontmatter/parse for unterminated frontmatter · ✓ reports frontmatter/parse for invalid YAML · ✓ reports frontmatter/parse for frontmatter is not a mapping (os 4 testes do `describe`, `rules.test.ts:16-36`) | verified at 75e4b94: mapeamento causa → regra em `packages/core/src/rules/frontmatter.ts:25-28` (`if (ctx.skill.frontmatter) return []` / um único `finding(..., "frontmatter/parse", 1, ctx.skill.frontmatterError ?? ...)`). Três causas asseridas na força do claim pelo `it.each` `packages/core/test/rules.test.ts:23-27`, SKILL.md gravado bruto em `:30`, `hit = f.filter((x) => x.rule === "frontmatter/parse")` em `:32`, `:33` - `expect(hit).toHaveLength(1)` e `:34` - `expect(hit[0].message).toContain(_cause)`: `unterminated frontmatter` (`frontmatter.ts:27`) - `:24` `"---\nname: mass-x\ndescription: never closed\n"`; `invalid YAML` (`frontmatter.ts:34`) - `:25` `"---\nname: [unclosed\n---\nbody\n"`; `frontmatter is not a mapping` (`frontmatter.ts:37`) - `:26` `"---\n- just\n- a list\n---\nbody\n"`. A quarta causa, `missing frontmatter` (`frontmatter.ts:25`), continua no teste pré-fix `:17-20`: `:18` `findingsFor("mass-x", { frontmatter: undefined, body: "no frontmatter", noSkillMd: true })`, `:19` - `expect(rules(f)).toContain("frontmatter/parse")` - assere só a presença do id, nem `toHaveLength(1)` nem `message`. `rg -n 'missing frontmatter' packages test apps` → só `frontmatter.ts:25`. Claim de 4 casos provado na força declarada em 3; o mutante Fh (`missing` rotulado `unterminated frontmatter`) sobrevive aos 4 testes e à suíte inteira do core (106 passed). Fe, Ff e Fg mortos | FAIL |
| C17 | chave fora das 6 → `frontmatter/unknown-key` | lote 1 ✓ unknown key › reports frontmatter/unknown-key for a key outside the spec (+ accepts every key of the spec) | verified at 75e4b94 (linhas re-citadas, +15 após o `it.each` do fix): `packages/core/test/rules.test.ts:41-42` - `f.filter((x) => x.rule === "frontmatter/unknown-key")` → `toHaveLength(1)`; `:52` - `not.toContain("frontmatter/unknown-key")` com as 6 chaves | PASS |
| C18 | `name` ≠ pasta, fora do regex, > 64 → `frontmatter/name` (3 casos) | lote 1 ✓ name rule (4 linhas `it.each` + accepts) | verified at 75e4b94 (linhas re-citadas, +15 após o `it.each` do fix): `packages/core/test/rules.test.ts:57-64` - `it.each([differs from the folder, does not match the mass- regex ×2, exceeds 64 characters])` → `expect(rules(f)).toContain("frontmatter/name")`; `:68` - aceita nome válido | PASS |
| C19 | `description` vazia, > 1024, fora da fórmula → `frontmatter/description` (3 casos) | lote 1 ✓ description rule (4 linhas + accepts) | verified at 75e4b94 (linhas re-citadas, +15 após o `it.each` do fix): `packages/core/test/rules.test.ts:73-80` - `it.each([is empty, exceeds 1024 characters, is off the formula, lacks the Do NOT clause])` → `toContain("frontmatter/description")`; `:84` - aceita a fórmula | PASS |
| C20 | `frontmatter/metadata` cobre os 9 casos | lote 1 ✓ metadata rule (11 linhas + absent + accepts) | verified at 75e4b94 (linhas re-citadas, +15 após o `it.each` do fix): `packages/core/test/rules.test.ts:89-109` - `it.each` com license≠CC-BY-4.0, author/version/category/tags/reviewed ausentes, version não semver, category desconhecida, reviewed futuro, valor não string (lista, número) → `toContain("frontmatter/metadata")`; `:118` - metadata ausente; `:113` - bloco válido aceito | PASS |
| C21 | `compatibility` > 500 → `frontmatter/compatibility` | lote 1 ✓ compatibility rule | verified at 75e4b94 (linhas re-citadas, +15 após o `it.each` do fix): `packages/core/test/rules.test.ts:125` - `toContain("frontmatter/compatibility")` com 501 chars; `:130` - `not.toContain(...)` com 500 | PASS |
| C22 | byte `0x00` nos primeiros 8192 → `content/binary` | lote 1 ✓ binary rule | verified at 75e4b94 (linhas re-citadas, +15 após o `it.each` do fix): `packages/core/test/rules.test.ts:137-139` - `hit = f.filter(rule === "content/binary")` → `toHaveLength(1)`, `hit[0].path === "skills/mass-x/assets/logo.png"`; `:144` - NUL após 8192 não é reportado | PASS |
| C23 | cada um dos 4 padrões de segredo → `security/secret` | lote 1 ✓ secret patterns (4 linhas) | verified at 75e4b94 (linhas re-citadas, +15 após o `it.each` do fix): `packages/core/test/rules.test.ts:149-158` - `it.each([aws access key, github token, private key, generic api key])` → `f.filter(rule === "security/secret")` `toHaveLength(1)`, `hit[0].line === 2` | PASS |
| C24 | cada um dos 5 padrões de shell → `security/shell` | lote 1 ✓ shell patterns (5 linhas + accepts) | verified at 75e4b94 (linhas re-citadas, +15 após o `it.each` do fix): `packages/core/test/rules.test.ts:163-173` - `it.each` com os 5 padrões → `toHaveLength(1)`; `:178` - curl sem pipe não é reportado | PASS |
| C25 | cada uma das 5 frases, sem caixa → `security/prompt-injection` | lote 1 ✓ prompt injection phrases (5 linhas) | verified at 75e4b94 (linhas re-citadas, +15 após o `it.each` do fix): `packages/core/test/rules.test.ts:183-191` - `it.each` com as 5 frases em caixa variada → `f.filter(rule === "security/prompt-injection")` `toHaveLength(1)` | PASS |
| C26 | script sem `#!` → `scripts/shebang`; sem `100755` no git → `scripts/executable` | lote 1 ✓ scripts rule (4 testes) | verified at 75e4b94 (linhas re-citadas, +15 após o `it.each` do fix): `packages/core/test/rules.test.ts:198` - `toContain("scripts/shebang")`; `:205` - `toContain("scripts/executable")` (modo fs); `:214` - `toContain("scripts/executable")` com `gitModes` `100644`; `:219-220` - aceita `#!` + bit | PASS |
| C27 | 3001 tokens → `size/tokens-warn` sem erro; 6001 tokens ou 501 linhas → `size/tokens` | lote 1 ✓ size rule (4 testes) | verified at 75e4b94 (linhas re-citadas, +15 após o `it.each` do fix): `packages/core/test/rules.test.ts:231-234` - `toHaveLength(1)`, `severity === "warn"`, `f.some(severity === "error") === false`; `:245-247` - 6001 tokens `size/tokens` error; `:254` - 501 linhas `size/tokens` error `toHaveLength(1)` | PASS |
| C28 | link relativo inexistente → `links/missing` | lote 1 ✓ missing link | verified at 75e4b94 (linhas re-citadas, +15 após o `it.each` do fix): `packages/core/test/rules.test.ts:261-264` - `toHaveLength(1)`, `message` contém `references/nope.md`; `:272` - links existentes/externos aceitos | PASS |
| C29 | `evals/` sem `triggers.json` com `should`/`shouldNot` → `evals/shape` | lote 1 ✓ evals shape (3 testes) | verified at 75e4b94 (linhas re-citadas, +15 após o `it.each` do fix): `packages/core/test/rules.test.ts:279` - `toContain("evals/shape")` sem triggers.json; `:284` - sem `shouldNot`; `:289` - aceita os dois arrays | PASS |
| C30 | `buildRegistry` na forma da door 3; `contentHash` bate com vetor conhecido | lote 1 ✓ registry shape follows door 3 · ✓ contentHash known answer | carried from 89f3d3a: `packages/core/test/registry.test.ts:41` - `files.map(path)` `toEqual(["SKILL.md", "assets/t.md", "references/a.md", "references/z.md"])`; `:43` - `sha256` `/^[0-9a-f]{64}$/`; `:36` - tags array; `:49` - `tokens === Math.ceil(raw.length / 4)`; `:29` - deprecated copiado; `:28` - generatedAt ISO UTC; `:64` - `expect(contentHash).toBe(EXPECTED)` com vetor de `sha256sum` (`:54-56`) | PASS |
| C31 | `registry --check` exit 1 listando campos divergentes, ignorando `generatedAt` | lote 1 ✓ --check detects drift and lists the divergent fields | carried from 89f3d3a: `packages/core/test/registry-bin.test.ts:25-28` - `expect(res.status).toBe(1)`, stdout contém `skills[0].files[0].sha256` e `skills[0].contentHash`, `not.toContain("generatedAt")` | PASS |
| C32 | deprecado com pasta, ou `replacedBy` inexistente → `deprecated/conflict` (2 casos) | lote 1 ✓ deprecated conflict (3 testes) | verified at 75e4b94 (linhas re-citadas, +15 após o `it.each` do fix): `packages/core/test/rules.test.ts:299-302` - `toHaveLength(1)`, `rule === "deprecated/conflict"`, `path === "skills/_deprecated.json"`; `:309-310` - replacedBy `mass-ghost`; `:316` - deprecação consistente `toEqual([])` | PASS |
| C33 | core exporta as 5 funções e tipos; cli/site não importam `gray-matter` nem `node:crypto` | lote 1 ✓ public api exposes the parse, validate, hash and registry functions · ✓ no duplicate parser | carried from 89f3d3a: `packages/core/test/exports.test.ts:21` - `expect(typeof core[fn], fn).toBe("function")` para `parseSkill`, `validateSkill`, `validateCatalog`, `hashFiles`, `buildRegistry`; `:29-30` - `not.toMatch(/from\s+["']gray-matter["']/)`, `not.toMatch(/from\s+["'](node:)?crypto["']/)` sobre `packages/cli/src` e `apps/site/src` | PASS |
| C34 | 5 skills, cada SKILL.md ≤ 80 linhas, `validateCatalog` sem achados | lote 1 ✓ five example skills validate and stay under 80 lines | carried from 89f3d3a: `test/repo/catalog.test.ts:14` - `expect(dirs).toEqual(FIVE)`; `:17` - `expect(lines, name).toBeLessThanOrEqual(80)`; `:20-21` - `result.skills` `toEqual(FIVE)`, `result.findings` `toEqual([])` | PASS |
| C35 | `mass-skill-authoring` com `references/` referenciado e `evals/triggers.json` ≥ 3/3 | lote 1 ✓ skill-authoring exercises references and evals | carried from 89f3d3a: `test/repo/catalog.test.ts:27-29` - `refs.length >= 1`, `body.includes(`references/${f}`)`; `:31-32` - `triggers.should.length >= 3`, `shouldNot.length >= 3` | PASS |
| C36 | `mass-commit-message/scripts/` com shebang e executável; `mass-pr-description/assets/` referenciado | lote 1 ✓ commit-message scripts and pr-description assets | carried from 89f3d3a: `test/repo/catalog.test.ts:40-41` - `startsWith("#!")`, `statSync(abs).mode & 0o111` `not.toBe(0)`; `:44-46` - `assets.length >= 1`, `body.includes(`assets/${a}`)` | PASS |
| C37 | `mass-code-review` com `metadata.requires: "git, gh"` e `allowed-tools`; `mass-security-checklist` só SKILL.md | lote 1 ✓ code-review requires and security-checklist minimal | carried from 89f3d3a: `test/repo/catalog.test.ts:51` - `expect(...requires).toBe("git, gh")`; `:52-53` - `allowed-tools` string não vazia; `:55` - `expect(minimal).toEqual(["SKILL.md"])` | PASS |
| C38 | `_categories.json` cobre toda categoria usada; `_deprecated.json` é `{}` | lote 1 ✓ categories and deprecated files | carried from 89f3d3a: `test/repo/catalog.test.ts:67` - `expect(ids, `${name} category`).toContain(meta.category)` para as 5; `:69` - `toEqual({})`; `:62-63` - `en`/`pt-br` strings | PASS |
| C39 | `marketplace.json` e `plugin.json` na forma da door 5 | lote 1 ✓ marketplace shape follows door 5 | carried from 89f3d3a: `test/repo/marketplace.test.ts:11` - `mkt.name === "mass-solutions"`; `:15-18` - `plugin.name === "mass-solutions-skills"`, `source === "./"`, `skills` `toEqual(["./skills/"])`, `license === "CC-BY-4.0"`; `:23-25` - `manifest.name`, `version` igual ao plugin, `author.name === "Mass Solutions"` | PASS |
| C40 | `npx skills add ./ --list` lista exatamente as 5 `mass-*` | `bash tools/spike/npx-skills-list.sh` exit 0 (`PASS: npx skills lists exactly: mass-code-review mass-commit-message mass-pr-description mass-security-checklist mass-skill-authoring`) | carried from 89f3d3a: `tools/spike/npx-skills-list.sh:12` - `grep -q "Found ${count} skill"` senão exit 1; `:15` - `[ "$listed" != "$expected" ]` → exit 1 (sobre export limpo da árvore, `:5`) | PASS |
| C41 | `claude plugin validate .` sai com 0 | `bash tools/spike/claude-plugin-validate.sh` exit 0 (`✔ Validation passed`) | carried from 89f3d3a: `tools/spike/claude-plugin-validate.sh:3` - `set -euo pipefail`; `:5` - `claude plugin validate .` (exit ≠ 0 aborta o script) | PASS |
| C42 | marketplace add + plugin install deixam as 5 pastas; script desfaz ao final | `bash tools/spike/claude-plugin-install.sh` exit 0 (`installPath: ~/.claude/plugins/cache/mass-solutions/mass-solutions-skills/0.1.0`; `PASS: plugin installs exactly: ...`; `claude plugin list --json` sem `mass-solutions-skills` depois) | carried from 89f3d3a: `tools/spike/claude-plugin-install.sh:19-24` - `claude plugin list --json` precisa conter `mass-solutions-skills@mass-solutions` senão exit 1; `:27` - `[ "$installed" != "$expected" ]` → exit 1; `:7-12` - cleanup uninstall + marketplace remove | PASS |
| C43 | `list` imprime `<name>  <version>  <category>  <description≤80>` ordenado por `name`, exit 0 (HTTP local) | lote 2 ✓ lists skills in name order with name, version, category and description up to 80 chars | carried from 89f3d3a: `packages/cli/test/list.test.ts:13` - `expect(r.code).toBe(0)`; `:15` - `toEqual(["mass-alpha", "mass-beta", "mass-gamma"])`; `:17` - `` expect(lines[1]).toBe(`mass-beta  0.1.0  quality  ${beta.description.slice(0, 80)}`) ``; `:19` - descrição original > 80 | PASS |
| C44 | `search` filtra name/description/tags sem caixa; sem resultado `No skills match "<termo>"`, exit 0 | lote 2 ✓ search matches case-insensitively on name, description and tags · ✓ search with no match prints the message and exits 0 | carried from 89f3d3a: `packages/cli/test/list.test.ts:24-25` - `BETA` → 1 linha `^mass-beta `; `:27` - `defects` por description; `:29` - `COMMITS` por tag → `["mass-alpha", "mass-gamma"]`; `:35-36` - `code === 0`, `expect(r.stdout).toBe('No skills match "zzz-nothing"\n')` | PASS |
| C45 | `install` grava em `<agent path>/<name>/` para os 8 agentes (projeto e `--global`) e registra no lockfile | lote 2 ✓ installs into every agent path, project and global, and records the lockfile entry | carried from 123d165: `packages/cli/test/install.test.ts:24` - `const cases = DOOR_10` (tabela literal de `packages/cli/test/door10.ts:2-11`, não derivada de `AGENTS`); `:25` - `expect(cases).toHaveLength(8)`; `:29-33` - `code === 0`, `SKILL.md` idêntico ao do catálogo, `references/guide.md` e `scripts/run.sh` presentes em `join(p.cwd, c.project, "mass-alpha")`; `:35` - `lock.skills["mass-alpha"].agents` `toEqual([c.id])`; `:38-40` - `--global` grava em `join(p.home, c.global, ...)` e `~/.config/mass-skills/lock.json`. Mutante Fa (F5 re-injetada) morto por esta prova | PASS |
| C83 | `AGENTS` é igual à door 10, comparada com tabela literal (8 ids + paths de projeto e global) | lote 2 ✓ agents table matches door 10: eight ids with their literal project and global paths | carried from 123d165: `packages/cli/test/agents.test.ts:7` - `expect(AGENTS.map((a) => ({ id: a.id, project: a.projectDir, global: a.globalDir }))).toEqual(DOOR_10)`; `:8` - `expect(DOOR_10).toHaveLength(8)`; `packages/cli/test/door10.ts:3-10` - os 8 pares literais, conferidos um a um contra a door 10 do plano (`plan.md:352`): `claude-code` `.claude/skills`/`.claude/skills`, `cursor` `.agents/skills`/`.cursor/skills`, `codex` `.agents/skills`/`.codex/skills`, `github-copilot` `.agents/skills`/`.copilot/skills`, `opencode` `.agents/skills`/`.config/opencode/skills`, `windsurf` `.windsurf/skills`/`.codeium/windsurf/skills`, `gemini-cli` `.agents/skills`/`.gemini/skills`, `cline` `.agents/skills`/`.agents/skills` - 16 valores iguais. Mutante Fa morto | PASS |
| C46 | `sha256` ou `contentHash` divergente → `Integrity check failed for <name>: <path>`, exit 1, nada gravado (2 casos) | lote 2 ✓ integrity failure writes nothing: file sha256 mismatch · ✓ integrity failure writes nothing: contentHash mismatch with every file sha256 intact | carried from 123d165: `packages/cli/test/install.test.ts:65-66` - `code === 1`, `expect(r.stderr).toBe("Integrity check failed for mass-alpha: references/guide.md\n")`, `untouched(p)` (`:14-20`); `:82-83` - `toBe("Integrity check failed for mass-alpha: contentHash\n")`, `untouched(p)` | PASS |
| C47 | falha de rede no 2º de 3 arquivos deixa agente e lockfile intactos | lote 2 ✓ partial download leaves nothing behind when the second of three files fails | carried from 123d165: `packages/cli/test/install.test.ts:96-98` - `code === 1`, stderr `^Failed to fetch http://127.0.0.1:\d+/main/skills/mass-alpha/references/guide.md: `, `fx.hits.get("skills/mass-alpha/SKILL.md") > 0`; `untouched(p)` (`:93` override `"fail"` = socket destruído, `helpers.ts:65-67`) | PASS |
| C48 | agente desconhecido → mensagem com lista e fallback `npx skills add maiconsouza89/agents-skills`, exit 2 | lote 2 ✓ unsupported agent exits 2 naming the supported list and the npx fallback | carried from 123d165: `packages/cli/test/install.test.ts:108-109` - `code === 2`, `expect(r.stderr).toBe('Unsupported agent "vim". Supported: claude-code, cursor, codex, github-copilot, opencode, windsurf, gemini-cli, cline. For other agents use: npx skills add maiconsouza89/agents-skills\n')`, `untouched(p)` | PASS |
| C49 | `-a auto` escolhe por `.claude/`, `.agents/`, `.windsurf/`; sem nenhuma → exit 2 com ids | lote 2 ✓ auto detects agents from .claude, .agents and .windsurf folders in the cwd · ✓ auto with nothing detected exits 2 listing the supported ids | carried from 123d165: `packages/cli/test/install.test.ts:119-121` - `.claude/skills/mass-gamma/SKILL.md` e `.windsurf/skills/...` existem, `.agents` não; `:123` - `agents` `toEqual(["claude-code", "windsurf"])`; `:128` - `.agents` → os 6 ids; `:141-142` - `code === 2`, stderr `^No agent detected in .*Supported: claude-code, ..., cline\n$` | PASS |
| C50 | deprecado → `"<old>" is deprecated since <since>: <reason>. Use: <replacedBy>`, exit 1; ausente → exit 2 sugerindo `search` | lote 2 ✓ deprecated is refused with the replacement and exit 1 · ✓ unknown skill exits 2 suggesting search | carried from 123d165: `packages/cli/test/install.test.ts:149-150` - `code === 1`, `toBe('"mass-old" is deprecated since 2026-01-01: renamed. Use: mass-alpha\n')`; `:157-158` - `code === 2`, `toBe('Unknown skill "mass-nope". Try: mass-skills search mass-nope\n')` | PASS |
| C51 | `..`, `/` inicial ou `name` fora do regex → `Unsafe path`, exit 1, nada gravado (3 casos) | lote 2 ✓ unsafe path is refused before anything is written: .., leading slash, bad name | carried from 123d165: `packages/cli/test/install.test.ts:177-179` - `expect(r.code, c.label).toBe(1)`, `toMatch(/^Unsafe path: /)`, nenhum hit além de `skills-registry.json` (3 casos `../escape.md`, `/etc/passwd`, `Mass-Alpha`, `:164-168`); `:180` - `untouched(p)` | PASS |
| C52 | lockfile na forma da door 4 via `.tmp` + rename, sem `.tmp` sobrando; não lê nem escreve `skills-lock.json` | lote 2 ✓ lockfile shape and atomic write: door 4 shape, tmp + rename, no tmp left, skills-lock.json untouched | carried from 89f3d3a: `packages/cli/test/lockfile.test.ts:22-25` - `Object.keys(lock)` `toEqual(["version", "skills"])`, `version === 1`, chaves da entrada `["version", "contentHash", "ref", "agents", "installedAt"]`, hash hex; `:26` - `readdirSync(p.cwd).filter(endsWith(".tmp"))` `toEqual([])`; `:27-28` - `skills-lock.json` intacto e `grill-me` ausente do lock | PASS |
| C53 | `update`: modificado → `locally modified, skipped (use --force)`; registry maior → reinstala; igual → `up to date` | lote 2 ✓ update three outcomes: locally modified is skipped, newer registry version reinstalls, equal is up to date | carried from 123d165: `packages/cli/test/update.test.ts:31` - `expect(r.stdout).toBe("mass-alpha: locally modified, skipped (use --force)\nmass-beta: updated to 0.2.0\nmass-gamma: up to date\n")`; `:32` - edição local preservada; `:34-35` - lock alpha `0.1.0`, beta `0.2.0` | PASS |
| C84 | `update`: pasta sumida → `<name>: missing (run mass-skills install)`; ausente do registry → `<name>: not in registry`; nada escrito | lote 2 ✓ update reports missing and not-in-registry and writes nothing | carried from 123d165: `packages/cli/test/update.test.ts:72` - `expect(r.code, r.stderr).toBe(0)`; `:73` - `expect(r.stdout).toBe("mass-alpha: missing (run mass-skills install)\nmass-beta: not in registry\n")`; `:74` - lockfile byte a byte igual; `:75` - `SKILL.md` de beta intacto; `:76` - pasta de alpha continua ausente (ramos `update.ts:30,34`, mensagens `:44-47`). Mutante Fb morto | PASS |
| C54 | `update --force` sobrescreve e grava `installedAt` novo | lote 2 ✓ force overwrites local edits and records a new installedAt | carried from 123d165: `packages/cli/test/update.test.ts:52` - `toBe("mass-alpha: overwritten to 0.1.0\n")`; `:53` - conteúdo igual ao do catálogo; `:55` - `installedAt` `toBe("2026-09-14T12:00:00.000Z")` | PASS |
| C55 | `update --check` só imprime estados e não escreve nada | lote 2 ✓ check writes nothing and prints every state | carried from 123d165: `packages/cli/test/update.test.ts:100` - `toBe("mass-alpha: locally modified\nmass-beta: update available 0.2.0\nmass-gamma: deprecated since 2026-02-02, use: mass-alpha\n")`; `:101-103` - lockfile, SKILL.md editado e mtime de beta inalterados; `:108` - `toContain("mass-beta: up to date\n")` | PASS |
| C56 | `doctor` reporta `ok`, `missing`, `modified`, `deprecated`; exit 1 se algum ≠ ok | lote 2 ✓ doctor reports every state: ok, missing, modified, deprecated, and exits 1 when any is not ok | carried from 89f3d3a: `packages/cli/test/doctor.test.ts:17-18` - `code === 0`, `toBe("mass-alpha: ok\nmass-beta: ok\nmass-gamma: ok\n")`; `:29-30` - `code === 1`, `toBe("mass-alpha: missing\nmass-beta: modified\nmass-gamma: deprecated\n")` | PASS |
| C57 | `remove` apaga só nos agentes registrados e remove a entrada, exit 0 | lote 2 ✓ removes only registered agents and drops the lockfile entry | carried from 89f3d3a: `packages/cli/test/remove.test.ts:22-25` - `code === 0`, `.claude/skills/mass-alpha` removido, cópia manual em `.windsurf/skills/mass-alpha/SKILL.md` preservada, `mass-beta` intacta; `:27` - `Object.keys(lock.skills)` `toEqual(["mass-beta"])`; `:29` - remover de novo → exit 2 | PASS |
| C58 | registry `404`, arquivo de `files[]` `404` (registry `200`), ou erro de rede → `Failed to fetch <url>: <status ou erro>`, exit 1, nada alterado (3 casos) | lote 2 ✓ fetch failure: registry 404 and network error exit 1 with the url and change nothing | carried from 123d165: `packages/cli/test/install.test.ts:192-193` - `code === 1`, `toMatch(/^Failed to fetch http:\/\/127\.0\.0\.1:\d+\/main\/skills-registry\.json: 404\n$/)`, `untouched(p)`; `:201-203` - rede (`127.0.0.1:1`) `code === 1`, `Failed to fetch ...: .+` e `not.toMatch(/: \d{3}\n$/)`; `:208` - `fx.overrides.set("skills/mass-alpha/references/guide.md", 404)` (servido como `res.writeHead(404).end("not found")`, `helpers.ts:61-64` - HTTP real, não socket destruído); `:211-212` - `code === 1`, `toMatch(/^Failed to fetch http:\/\/127\.0\.0\.1:\d+\/main\/skills\/mass-alpha\/references\/guide\.md: 404\n$/)`; `:213` - `untouched(f)`. Mutante Fd morto | PASS |
| C59 | exit 0/1/2 conforme door 11; erros só em `stderr` | lote 2 ✓ exit code contract: 0 success, 1 execution failure, 2 usage error, errors on stderr only | carried from 123d165: `packages/cli/test/exit-codes.test.ts:33` - `expect(r.code, row.argv.join(" ")).toBe(row.code)` sobre a tabela `:13-26` (3×0, 3×1, 8×2 - inclui `update --bogus` `:24`, `doctor --bogus` `:25`, `remove` sem args `:26`); `:34` - sucesso → `stderr === ""`; `:36-37` - falha → `stderr.length > 0`, `stdout === ""` | PASS |
| C60 | `mass-skills validate` e `registry --check` espelham os exit codes dos scripts da raiz | lote 2 ✓ validate and registry mirror root scripts: same exit codes on a valid and an invalid catalog | carried from 89f3d3a: `packages/cli/test/mirrors.test.ts:24` - `expect(viaCli.code, root).toBe(core)` (catálogo bom e ruim); `:26-27` - `0` e `1`; `:30-37` - `registry --check` 1 / 0 / 1 tanto em `registryMain` quanto via CLI | PASS |
| C82 | sem `MASS_SKILLS_BASE_URL`, base `https://raw.githubusercontent.com/maiconsouza89/agents-skills/<ref>/`, ref default `main` | lote 2 ✓ default base url is the raw github url of the catalog repo with ref main | carried from 89f3d3a: `packages/cli/test/download.test.ts:6` - `expect(DEFAULT_BASE_URL).toBe("https://raw.githubusercontent.com/maiconsouza89/agents-skills/")`; `:7` - `baseUrl({}) === DEFAULT_BASE_URL`; `:8` - `registryUrl({}, "main")` = `.../main/skills-registry.json`; `:9-11` - `fileUrl` (assembly real: `packages/cli/src/download.ts:4,9-13`) | PASS |
| C61 | build gera as rotas EN e sob `/pt-br/`, `search-index.json`, `404.html`; links internos começam com `/agents-skills/` | lote 3 ✓ generates every route under the base for en and pt-br (astro build real, `apps/site/test/helpers.ts:36`) | carried from 123d165: `apps/site/test/build.test.ts:26` - `expect(files, e).toContain(e)` para `404.html`, `index.html`, `install/`, `agents/`, `search-index.json`, `pt-br/...`, `skills/<n>/` ×5 e `pt-br/skills/<n>/` ×5; `:27` - exatamente 5 sob `skills/`; `:31` - todo `href`/`src` absoluto `toMatch(^/agents-skills/)` (`helpers.ts:10` `BASE`); `:34` - índice com os 5 nomes | PASS |
| C62 | `content.config.ts` usa `glob()` com `base` no catálogo; sem cópia de SKILL.md em `apps/site/` | lote 3 ✓ reads the catalog in place through glob() and never copies SKILL.md into the site | carried from 123d165: `apps/site/test/build.test.ts:39-41` - `toContain("glob({")`, `toMatch(/pattern:\s*"\*\/SKILL\.md"/)`, `toMatch(/base:\s*CATALOG_ROOT/)`; `:43` - `astro.config.mjs` contém `process.env.MASS_CATALOG_ROOT ||= src("../../skills")`; `:45` - `expect(copies).toEqual([])` | PASS |
| C63 | home agrupa por categoria (rótulo do idioma), ordena por `name`, mostra name/description/version, busca, filtro e troca de idioma | lote 3 ✓ home lists grouped and ordered skills with search, filter and a language switch | carried from 123d165: `apps/site/test/build.test.ts:56` - seções `data-category` `toEqual(usedIds)`; `:61` - `h2` contém o rótulo `en`/`pt-br`; `:64` - `expect(itemNames).toEqual([...itemNames].sort())`; `:68-71` - href localizado, name, description, version; `:76` - `input#q`; `:78` - options `["", ...usedIds]`; `:79` - `[data-lang-switch]` href `toBe(switchTo)` | PASS |
| C64 | busca sem resultado mostra `No skills match "<termo>"` / `Nenhuma skill corresponde a "<termo>"` sem esconder o campo (jsdom com script real) | lote 3 ✓ no match message names the term and keeps the field visible | carried from 89f3d3a: `apps/site/test/search.test.ts:45-46` - `expect(noMatch.hidden).toBe(false)`, `expect(noMatch.textContent).toBe(message)` (EN e PT, `:37-40`); `:47` - `input#q` `.hidden === false`; `:48` - todos `[data-skill]` ocultos; `:50-53` - busca com resultado volta a esconder a mensagem | PASS |
| C65 | página da skill com 3 partes da description, corpo EN nas duas rotas, metadata, files com bytes, contentHash, link fonte, painel com 3 comandos e aviso | lote 3 ✓ skill page content: description parts, body in english on both routes, metadata, files, hash, source and the install panel | carried from 123d165: `apps/site/test/build.test.ts:92-94` - `[data-what]`, `[data-when]`, `[data-not]` com texto exato; `:96` - `body.getAttribute("lang") === "en"`; `:114` - `expect(bodies[0]).toBe(bodies[1])`; `:100-102` - author/version/reviewed/tags, `[data-requires] === "git, gh"`, `[data-allowed-tools]`; `:104-106` - `[data-file]` = `reg.files` com `bytes`, `[data-content-hash] === reg.contentHash`; `:107` - `[data-source]` = `https://github.com/${REPO}/tree/main/skills/<name>`; `:109-112` - os 3 comandos e o aviso de hash | PASS |
| C66 | skill mínima omite seção de arquivos adicionais e lista só `SKILL.md` | lote 3 ✓ minimal skill omits extra files section and lists only SKILL.md; a skill with references/ shows it | carried from 123d165: `apps/site/test/build.test.ts:119` - `expect(minimal.querySelector("[data-extra-files]")).toBeNull()`; `:120` - `[data-file]` `toEqual(["SKILL.md"])`; `:122` - skill rica mostra `references/` | PASS |
| C67 | nome deprecado gera `/skills/<old>/` com `Deprecated since <since>`, `replacedBy` e sem painel (fixture via `MASS_CATALOG_ROOT`) | lote 3 ✓ deprecated page shows the banner, the replacement and no install panel | carried from 89f3d3a: `apps/site/test/deprecated.test.ts:21-22` - `[data-deprecated]` contém `Deprecated since 2026-08-01` e o motivo; `:24-25` - link `mass-alpha` para `/agents-skills/[pt-br/]skills/mass-alpha/`; `:26-27` - `[data-install]` `toBeNull()`, `main` não contém `mass-skills install`; `:29` - página viva mantém o painel (assembly: `deprecated.test.ts:17` `MASS_CATALOG_ROOT`, `apps/site/src/lib/paths.ts:5-9`) | PASS |
| C68 | `/install/` com 3 caminhos em ordem e o que cada um verifica; `/agents/` com os 8 agentes, path de projeto e global | lote 3 ✓ install and agents pages: three paths in order with what each verifies, and the eight agents with both paths | carried from 123d165: `apps/site/test/build.test.ts:132` - títulos `toEqual(["1. mass-skills CLI", "2. npx skills add", "3. Claude Code marketplace"])` (PT `:128`); `:133-135` - `sha256`, `Nothing is verified`, `No hash verification`; `:140` - linhas `data-agent` `toEqual(DOOR_10.map((a) => a.id))`; `:143-144` - `` toContain(`${a.project}/`) ``, `` toContain(`~/${a.global}/`) `` com `DOOR_10` literal (`:4` importa `packages/cli/test/door10.ts`, não `AGENTS`). Mutante Fa morto também aqui (`Expected: "~/.cursor/skills/"`, `Received: "cursor.agents/skills/~/.cursorx/skills/"`) | PASS |
| C69 | `/404.html` liga para `/` e `/pt-br/` | lote 3 ✓ 404 page links to both catalogs | carried from 123d165: `apps/site/test/build.test.ts:152-153` - `expect(hrefs).toContain(BASE)`, `` toContain(`${BASE}pt-br/`) `` (`BASE = "/agents-skills/"`) | PASS |
| C70 | CSS: monoespaçada no `body`, `--color-canvas: #201d1d` fundo, `--color-ink: #fdfcfc` cor, `border-radius: 4px` só em interativos, sem `box-shadow`/`gradient`; home usa `[+]`/`[-]` | lote 3 ✓ design tokens: monospace everywhere, dark canvas, light ink, 4px radius only on interactive elements, no shadow or gradient, ascii markers | carried from 123d165: `apps/site/test/build.test.ts:158-159` - `toMatch(/--color-canvas:#201d1d/)`, `toMatch(/--color-ink:#fdfcfc/)`; `:160-162` - `body{...background:var(--color-canvas)`, `color:var(--color-ink)`, `font-family:var(--font-mono)`; `:163` - `--font-mono` termina em `monospace`; `:164-165` - `not.toMatch(/box-shadow/)`, `not.toMatch(/gradient/)`; `:170-171` - todo bloco com `border-radius` tem seletor `^(input|select|.button|.snippet|.badge)` e valor `4px`/`var(--radius-sm)`; `:174-175` - home contém `[+]` e `[-]` | PASS |
| C71 | nenhuma página além da home tem `<script`; a home tem exatamente um | lote 3 ✓ zero client js outside home, one script on the home page | carried from 123d165: `apps/site/test/build.test.ts:181-182` - `expect(count, f).toBe(1)` para `index.html`/`pt-br/index.html`, `toBe(0)` para todas as outras; `:184` - script referencia `search-index.json` | PASS |
| C72 | `ci.yml`: `pull_request` + `push` em `main`, `node-version-file: .nvmrc`, cache pnpm, install/check/build/`plugin validate`, sem `secrets.` | lote 1 ✓ ci workflow runs check, build and the marketplace validation on PRs and main without secrets | carried from 89f3d3a: `test/repo/workflows.test.ts:26-27` - `Object.keys(wf.on).sort()` `toEqual(["pull_request", "push"])`, `push.branches` `toEqual(["main"])`; `:17-20` - `node-version-file === ".nvmrc"`, `cache === "pnpm"`, `pnpm/action-setup@`, `pnpm install --frozen-lockfile`; `:31-34` - `pnpm check`, `pnpm build`, `npx -y @anthropic-ai/claude-code@latest plugin validate .`, `not.toContain("secrets.")` | PASS |
| C73 | `pages.yml`: push em `main`, `pages: write` + `id-token: write`, `withastro/action` `path: apps/site`, `package-manager: pnpm@11`, node do `.nvmrc`, `actions/deploy-pages` | lote 1 ✓ pages workflow deploys the astro site from apps/site with the nvmrc node version | carried from 89f3d3a: `test/repo/workflows.test.ts:39-41` - branches `["main"]`, `permissions.pages === "write"`, `id-token === "write"`; `:43-45` - `path === "apps/site"`, `package-manager === "pnpm@11"`, `node-version` contém `steps.node.outputs.version`; `:46` - passo `cat .nvmrc`; `:47-48` - `actions/deploy-pages@` com `needs: build` | PASS |
| C74 | `security-scan.yml`: push `main` + `pull_request`; Snyk condicionado a `push` ou PR do mesmo repo; `uvx snyk-agent-scan@latest skills --ci` com `SNYK_TOKEN` e flags da allowlist | lote 1 ✓ security scan workflow runs snyk only on push to main and same-repo PRs, with the allowlist flags | carried from 89f3d3a: `test/repo/workflows.test.ts:53-54` - `push.branches` `["main"]`, `"pull_request" in wf.on`; `:61-63` - `run` contém `uvx snyk-agent-scan@latest skills --ci` e `steps.allowlist.outputs.flags`, `env.SNYK_TOKEN === "${{ secrets.SNYK_TOKEN }}"`; `:64-67` - `snyk.if` e `uv.if` contêm a condição de push/mesmo repo | PASS |
| C75 | allowlist vencida → exit 1 `Allowlist entry expired: <risk> <skill> <expiresAt>`; vigentes → `--ignore-risks a,b`; passo sem condição no workflow | lote 1 ✓ expired entry fails with the entry named and no flags printed · ✓ valid entries become ignore flags, and an empty allowlist prints nothing (+ ✓ security scan workflow) | carried from 89f3d3a: `test/repo/allowlist.test.ts:32-34` - `main(...)` `toBe(1)`, `stderr` `toBe("Allowlist entry expired: prompt-injection mass-alpha 2026-09-01\n")`, `stdout === ""`; `:52-53` - `toBe(0)`, `stdout` `toBe("--ignore-risks prompt-injection,dangerous-download\n")`; `:56-57` - vazia → nada; `test/repo/workflows.test.ts:58` - `expect(allow.if).toBeUndefined()` | PASS |
| C76 | `stale-skills.yml`: `cron: "0 9 * * 1"` + `workflow_dispatch`; cria ou atualiza issue `Stale skills` com label `stale-skill` a partir de `pnpm stale` | lote 1 ✓ stale skills workflow runs weekly and on demand, and creates or updates the Stale skills issue from pnpm stale | carried from 89f3d3a: `test/repo/workflows.test.ts:72-73` - `schedule` `toEqual([{ cron: "0 9 * * 1" }])`, `"workflow_dispatch" in wf.on`; `:78` - `toMatch(/pnpm (--silent )?stale --days 90/)`; `:80-84` - script contém `const title = "Stale skills"`, `const label = "stale-skill"`, `issues.create`, `issues.update`, `labels: [label]` | PASS |
| C77 | saída vazia → não cria issue e fecha a aberta | lote 1 ✓ stale workflow closes when nothing is stale and creates nothing | carried from 89f3d3a: `test/repo/workflows.test.ts:90` - `toMatch(/if \(!list\) \{\s*if \(existing\) await github\.rest\.issues\.update\([^)]*state: "closed"[^)]*\);\s*return;/)` | PASS |
| C78 | `pnpm stale --days 90` imprime `<name> - reviewed <date> (<n> days)` por vencida, nada quando não há; exit 0 | lote 1 ✓ lists stale skills older than --days with the reviewed date and the age · ✓ prints nothing when none is stale and still exits 0 | carried from 89f3d3a: `test/repo/stale.test.ts:18-19` - `code === 0`, `expect(r.out).toBe("mass-old - reviewed 2026-06-15 (91 days)\n")` (limite de 90 dias exato excluído, `:15`); `:21` - `--days 10` lista 3; `:28-29` - `code === 0`, `out === ""` | PASS |
| C79 | `.changeset/config.json` com `baseBranch: "main"`, ignora `site`; pacotes `@mass-solutions/skills-core` e `@mass-solutions/skills-cli` | lote 1 ✓ changesets config targets main, ignores the site, and the two packages carry the scoped names | carried from 89f3d3a: `test/repo/release.test.ts:12-13` - `config.baseBranch === "main"`, `config.ignore` `toEqual(["site"])`; `:14-15` - nomes dos pacotes; `:16` - `apps/site` chama-se `site` | PASS |
| C80 | `release.yml` em tags `v*`, roda `pnpm check`, `pnpm build`, `gh release create <tag> --generate-notes` | lote 1 ✓ release workflow runs on v* tags, checks, builds and creates a github release with generated notes | carried from 89f3d3a: `test/repo/workflows.test.ts:95` - `wf.on.push.tags` `toEqual(["v*"])`; `:100-102` - `pnpm check`, `pnpm build`, `gh release create "${{ github.ref_name }}" --generate-notes` | PASS |
| C81 | registry commitado tem `version: 1` e `repo: "maiconsouza89/agents-skills"` | lote 1 ✓ committed registry version and repo | carried from 89f3d3a: `packages/core/test/registry.test.ts:69-70` - `expect(reg.version).toBe(1)`, `expect(reg.repo).toBe("maiconsouza89/agents-skills")` sobre `skills-registry.json` da raiz | PASS |

Todos os 86 testes/scripts nomeados existem na árvore e apareceram individualmente como `✓` na saída dos lotes (104 + 25 + 11 = 140 testes passados, 0 falhos; os 3 testes novos do `it.each` de C86 estão dentro do range `0fff8f7..75e4b94`). Os testes marcados `↓ skipped` são testes fora do filtro `-t`, não nomeados por nenhum check: lote 1 - `validate bin › exits 0 with nothing on stdout when the catalog is clean`, `registry bin › --check ignores generatedAt`; lote 2 - `install › lockfile entry carries version, contentHash, ref, agents and installedAt from the registry`, `lockfile › global lockfile lives under XDG_CONFIG_HOME or ~/.config`, `lockfile › writeLock leaves no tmp file and readLock round-trips`, `download base url › MASS_SKILLS_BASE_URL overrides the base and a ref is inserted after it`.

## Coverage

Linhas `verified at 75e4b94` foram recomputadas nesta rodada a partir da autoridade de cada conjunto: as duas cuja autoridade o fix tocou (`frontmatter parse causes`, de `packages/core/src/frontmatter.ts`; `validator rule ids`, de `packages/core/src`). As linhas cujas provas vivem em `rules.test.ts` foram herdadas com as linhas re-citadas (+15). As demais `carried from 123d165` ou `carried from 89f3d3a` porque o fix não tocou código de produção nem os testes que as provam.

| Set (size) | Recomputed from | Member -> proof | Unproven |
| --- | --- | --- | --- |
| validator rule ids (17) - verified at 75e4b94 | `rg -o '"[a-z]+/[a-z-]+"' packages/core/src \| sort -u` → 17 ids em `rules/frontmatter.ts` (6), `rules/content.ts` (8), `rules/size.ts` (2), `validate.ts` (1); `rg -o --no-filename` sobre `rules.test.ts` + `validate-bin.test.ts` → os mesmos 17 (`wc -l` = 17) | `frontmatter/parse` C86 `rules.test.ts:19` e `:32-33` · `frontmatter/unknown-key` C17 `:41-42` · `frontmatter/name` C18 `:64` · `frontmatter/description` C19 `:80` · `frontmatter/metadata` C20 `:109` · `frontmatter/compatibility` C21 `:125` · `content/binary` C22 `:137-138` · `security/secret` C23 `:156-157` · `security/shell` C24 `:171-172` · `security/prompt-injection` C25 `:191` · `scripts/shebang` C26 `:198` · `scripts/executable` C26 `:205,214` · `size/tokens-warn` C27 `:231-232` · `size/tokens` C27 `:245-246` · `links/missing` C28 `:261-262` · `evals/shape` C29 `:279` · `deprecated/conflict` C32 `:300` | - |
| frontmatter parse causes (4) - verified at 75e4b94 (linha acrescentada ao checks.md pelo fix: "one asserted case each") | `packages/core/src/frontmatter.ts:25` (`missing frontmatter`), `:27` (`unterminated frontmatter`), `:34` (`invalid YAML: ...`), `:37` (`frontmatter is not a mapping`) - os 4 retornos de `parseSkill` sem `frontmatter`, todos com `frontmatterError`, todos mapeados em `frontmatter/parse` por `rules/frontmatter.ts:25-28`; não há quinta causa (o fallback `?? "invalid frontmatter"` de `rules/frontmatter.ts:27` é inalcançável) | `unterminated frontmatter` C86 `rules.test.ts:24,33-34` (contagem 1 + mensagem; Fe e Fg mortos) · `invalid YAML` C86 `:25,33-34` (Ff morto) · `frontmatter is not a mapping` C86 `:26,33-34` · `missing frontmatter` C86 `:18-19` - caso existe, mas assere só `toContain("frontmatter/parse")`: a contagem e a mensagem que o claim de C86 exige para cada causa não são asseridas para esta (`rg -n 'missing frontmatter'` → 0 hits em testes; mutante Fh sobrevive) | `missing frontmatter` (`frontmatter.ts:25`) - provado só como id presente, não como "exatamente um `frontmatter/parse` cuja mensagem nomeia a causa" (claim de C86); mutante Fh sobrevivente |
| agent adapters (8) - carried from 123d165 | ids e paths: **plano door 10** (`plan.md:352`), transcrita literalmente em `packages/cli/test/door10.ts:3-10` e conferida célula a célula nesta rodada contra a door (16 valores iguais); o código (`agents.ts:14-23`) é o que deve satisfazê-la | ids: C83 `agents.test.ts:7` (`toEqual(DOOR_10)`) e C48 `install.test.ts:109` lista literal · path de projeto e global dos 8, literais: C83 `agents.test.ts:7` na própria camada, C45 `install.test.ts:24,30,39` na fronteira (fs), C68 `build.test.ts:140-144` no site · `-a auto` C49 `:119-135` · mutante Fa (F5 re-injetada, `cursor` global `.cursor/skills` → `.cursorx/skills`) morto pelas três provas | - |
| update outcomes (6) - carried from 123d165 | `packages/cli/src/commands/update.ts:14-20` (`UpdateState`: `missing`, `not-in-registry`, `deprecated`, `locally-modified`, `update-available`, `up-to-date`); ramos em `:30,32,34,37,38,39`; mensagens `:42-57`; `--force` `:76-78` | `locally-modified` C53 `update.test.ts:31` · `update-available` C53 `:31` (`updated to 0.2.0`) · `up-to-date` C53 `:31` · `missing` C84 `:73` · `not-in-registry` C84 `:73` · `deprecated` C55 `:100` (`deprecated since 2026-02-02, use: mass-alpha`, via `--check`; o caminho sem `--check` imprime o mesmo `describe(state)` em `update.ts:81-83`, ramo provado por C84 para os outros dois kinds) · `--force` C54 `:52-55`. A linha do checks.md conta `--force` como membro e omite `deprecated`; recomputado do código, os 6 kinds têm prova | - |
| `mass-skills update`/`doctor` exits (3) - carried from 123d165 | plano Surface; `run.ts:88-97` (`CliError.exitCode`, `CommanderError` → `EXIT_USAGE`) | 0 C55 `update.test.ts:72,99` e C84 `:72` · 1 C56 `doctor.test.ts:29` · 2 C59 `exit-codes.test.ts:24-26,33` (`update --bogus`, `doctor --bogus`, `remove` sem args, cada linha asserida em `:33` com `stderr` não vazio e `stdout` vazio em `:36-37`) - lacuna 2 da rodada 1 fechada | - |
| `GET raw/.../skills/<name>/<path>` statuses (2) - carried from 123d165 | plano Surface; `download.ts:32` (`if (!res.ok) throw ... ${res.status}`) | 200 C45 `install.test.ts:29-33` · 404 C58 `install.test.ts:208-213` (override `404` servido por `helpers.ts:61-64` como `res.writeHead(404)`, com registry `200`; mensagem `.../skills/mass-alpha/references/guide.md: 404`) - lacuna 4 da rodada 1 fechada; mutante Fd (404 tratado como sucesso) morto | - |
| CLI exit codes (3) - carried from 123d165 | `packages/cli/src/types.ts:14-18` | `0` C59 `exit-codes.test.ts:13-15` · `1` C59 `:16-18` · `2` C59 `:19-26` (8 linhas: `install` com agente/skill desconhecidos, comando inexistente, `install`/`remove` sem args, `--bogus` em `list`/`update`/`doctor`) | - |
| secret patterns (4) - carried from 89f3d3a, linhas de `rules.test.ts` re-citadas em 75e4b94 (+15) | `packages/core/src/constants.ts:24-29` | C23, `rules.test.ts:149-153` table-driven sobre os 4 ids | - |
| shell patterns (5) - carried from 89f3d3a, linhas de `rules.test.ts` re-citadas em 75e4b94 (+15) | `constants.ts:31-37` | C24, `rules.test.ts:163-168` sobre os 5 | - |
| prompt-injection phrases (5) - carried from 89f3d3a, linhas de `rules.test.ts` re-citadas em 75e4b94 (+15) | `constants.ts:39-45` | C25, `rules.test.ts:183-188` sobre as 5 | - |
| metadata cases (9) - carried from 89f3d3a, linhas de `rules.test.ts` re-citadas em 75e4b94 (+15) | AC 20 + `rules/frontmatter.ts:70-107` | C20, `rules.test.ts:89-99` (11 linhas) e `:118` | - |
| name cases (3) - carried from 89f3d3a, linhas de `rules.test.ts` re-citadas em 75e4b94 (+15) | AC 18 + `rules/frontmatter.ts:45-47` | C18, `rules.test.ts:57-61` | - |
| description cases (3) - carried from 89f3d3a, linhas de `rules.test.ts` re-citadas em 75e4b94 (+15) | AC 19 + `rules/frontmatter.ts:56-60` | C19, `rules.test.ts:73-77` | - |
| size thresholds (3) - carried from 89f3d3a, linhas de `rules.test.ts` re-citadas em 75e4b94 (+15) | `constants.ts:19-21` | C27, `rules.test.ts:228-255` | - |
| CLI commands (8) - carried from 89f3d3a | `packages/cli/src/run.ts:40-76` | `list` C43 · `search` C44 · `install` C45 · `update` C53 · `remove` C57 · `doctor` C56 · `validate` C60 · `registry` C60 | - |
| install refusals (5) - carried from 89f3d3a | `installer.ts:20,23,28,30,44,50`, `agents.ts:50,57` | integrity C46 · unsupported agent C48 · deprecated C50 · unknown skill C50 · unsafe path C51 | - |
| unsafe path cases (3) - carried from 89f3d3a | `paths.ts:4-14` | `..` C51 `:165` · leading `/` `:166` · bad name `:167` | - |
| doctor states (4) - carried from 89f3d3a | `commands/doctor.ts:7,15,18-20` | `ok` C56 `:18` · `missing`/`modified`/`deprecated` C56 `:30` | - |
| `GET raw/.../skills-registry.json` statuses (2) - carried from 89f3d3a | plano Surface; `download.ts:32` | 200 C43 `list.test.ts:13` · 404 C58 `install.test.ts:192-193` | - |
| `mass-skills list`/`search` exits (3) - carried from 89f3d3a | plano Surface; `run.ts:88-97` | 0 C43 `:13` · 1 C58 `install.test.ts:201` · 2 C59 `exit-codes.test.ts:23` | - |
| `mass-skills install` exits (3) - carried from 89f3d3a | plano Surface | 0 C45 `:29` · 1 C46 `:65` · 2 C48 `:108` | - |
| site routes under `/agents-skills/` (8) - carried from 89f3d3a | plano Surface; `apps/site/src/pages/**` | `/` C63 · `/pt-br/` C61 + C63 · `/skills/<name>/` C65 · `/pt-br/skills/<name>/` C61 + C65 · `/install/` C68 · `/agents/` C68 · `/search-index.json` C61 · `/404.html` C69 | - |
| site route statuses (2) - carried from 89f3d3a | plano Surface | 200 C61 `:26` · 404 C69 `:152-153` (site estático; o status é do GitHub Pages) | - |
| `.claude-plugin/marketplace.json` readers (3) - carried from 89f3d3a (os 3 scripts re-rodados nesta rodada, exit 0) | plano Surface + door 5 | Claude Code validate C41 · Claude Code install C42 · `npx skills` C40 | - |
| workflows (5) - carried from 89f3d3a | `ls .github/workflows` | `ci.yml` C72 · `pages.yml` C73 · `security-scan.yml` C74 · `stale-skills.yml` C76/C77 · `release.yml` C80 | - |
| root scripts (8) - carried from 89f3d3a (`pnpm validate` re-rodado, exit 0) | `package.json:13-21` | presença dos 8: C2 · `validate` C15/C16/C60 · `registry` C31/C60 · `build` C61 · `new-skill` C3/C4/C85 · `stale` C78 · `check`/`test`: presença e encadeamento C2 · `scan`: presença C2 e wiring C74, não executado (exige `SNYK_TOKEN`, declarado) | - |
| example skills (5) - carried from 89f3d3a | `ls skills/` | C34-C37 | - |
| one-way doors (11) - carried from 123d165 para a door 10, carried from 89f3d3a para as demais | plano Landing | 1 C15 · 2 C17-C21, C86 · 3 C30 · 4 C52 · 5 C39 · 6 C32/C38 · 7 C1 · 8 C5 · 9 C82 + C61 + C48/C6 · **10 C83 `agents.test.ts:7` + `door10.ts:3-10` (paths literais da door asseridos - lacuna 1 da rodada 1 fechada)** · 11 C59 | - |
| entities (8) - carried from 89f3d3a | plano Relations | `Skill` C30/C18 · `SkillFile` C30 · `Registry` C30/C81 · `Deprecation` C32/C50/C67 · `Lockfile` C52 · `LockEntry` C52 · `Agent` C45/C48/C83 · `Category` C20/C38/C63 | - |
| startup config: download base (2 assemblies) - carried from 89f3d3a | `download.ts:4,9-13`, `bin.ts:4-9` | bin default C82 · harness via `MASS_SKILLS_BASE_URL` C43 | - |
| startup config: catalog root for the site (2 assemblies) - carried from 89f3d3a | `astro.config.mjs:8`, `apps/site/src/lib/paths.ts:5-9` | default C62 + C61 · fixture C67 | - |
| `-a auto` markers (3) - carried from 89f3d3a | `agents.ts:28-32` | C49 `:119-135` | - |
| `update --check` states (4 no AC 55) - carried from 123d165 | AC 55; `update.ts:42-57` | `locally modified`, `update available <v>`, `deprecated` C55 `:100` · `up to date` C55 `:108` · os dois estados extra do código (`missing`, `not in registry`) agora asseridos por C84 `:73` no caminho sem `--check` (mesma função `describe`) | - |

Varredura por conjuntos sem linha: a linha `frontmatter parse causes (4)` que a rodada 2 apontou como ausente agora existe no checks.md (acrescentada pelo fix, membros idênticos aos recomputados do parser). Todo conjunto nomeado em `Landing`, `Relations` e `Surface` do plano continua com linha; as linhas de prosa abaixo da tabela do checks.md conferem no novo HEAD, exceto a contagem "16 rule ids" na evidência do `## Test policy` (ver observações).

## Test policy rows

Linha `verified at 75e4b94`: a que classifica `packages/core/src/rules/*.ts` (o fix tocou `rules.test.ts`, a prova que a sustenta, e a superfície do parser). As demais `carried from 123d165` (re-julgadas na rodada 2) ou `carried from 89f3d3a` - o fix não tocou nem os arquivos que classificam nem os testes que as provam. Célula `Expectation met` só com `yes`/`no`.

| Row | Files it classifies | Required proof | Expectation met |
| --- | --- | --- | --- |
| Decides, reached across a boundary - carried from 123d165 | `packages/cli/src/commands/install.ts` + `installer.ts` | fronteira: C45-C51, C58 via HTTP local e fs; cada linha da tabela de decisão tem caso asserido: agente explícito/auto/desconhecido (C45 `:29-40` com `DOOR_10`, C49, C48), sha256 e contentHash (C46 `:65-66,:82-83`), deprecada/ausente (C50), 3 paths inseguros (C51), falha no meio (C47), 404 de arquivo (C58 `:211-213`) | yes |
| Decides, reached across a boundary - carried from 123d165 | `packages/cli/src/agents.ts` (tabela da door 10) | própria camada: C83 `agents.test.ts:7` compara `AGENTS` com a tabela literal `door10.ts:3-10`, conferida contra `plan.md:352`; fronteira: C45 `install.test.ts:24` itera `DOOR_10`, não `AGENTS`. Mutante Fa (`agents.ts:16` `.cursor/skills` → `.cursorx/skills`) morto por C83, C45 e C68 | yes |
| Decides, not reached across a boundary - carried from 123d165 | `packages/cli/src/commands/update.ts` (`classify`, 6 kinds em `update.ts:14-20`; `--force`; `--check`) | C53-C55 asserem `locally-modified`, `update-available`, `up-to-date`, `deprecated` (`update.test.ts:31,:100`), `--force` (`:52-55`), `--check` (`:101-103`); C84 `:73-76` assere `missing` (`update.ts:30`) e `not-in-registry` (`:34`) com nada escrito. Mutante Fb morto | yes |
| Decides, not reached across a boundary - carried from 123d165 | `tools/new-skill.ts` (nome inválido, pasta já existe, validação do scaffold) | C3 `new-skill.test.ts:28-42`, C4 `:60-62`, C85 `:51-53` (ramo `already exists`, `new-skill.ts:17-20`). Mutante Fc morto | yes |
| Decides, not reached across a boundary - verified at 75e4b94 | `packages/core/src/rules/*.ts` (17 ids; a linha de evidência do checks.md ainda escreve "16 rule ids") | C17-C29, C32, C86 na própria camada; cada id com pelo menos um caso positivo asserido em `rules.test.ts` (re-citado no HEAD: `frontmatter/parse` `:19,:33` · `unknown-key` `:42` · `name` `:64` · `description` `:80` · `metadata` `:109` · `compatibility` `:125` · `content/binary` `:138` · `secret` `:157` · `shell` `:172` · `prompt-injection` `:191` · `shebang` `:198` · `executable` `:205` · `tokens-warn` `:232` · `tokens` `:246` · `links/missing` `:262` · `evals/shape` `:279` · `deprecated/conflict` `:300`), padrões table-driven cobrem `constants.ts`. A linha conta linhas da tabela de decisão de cada regra: `frontmatterParse` (`rules/frontmatter.ts:25-28`) tem uma, asserida. O parser `frontmatter.ts` (4 saídas) não é classificado por nenhuma linha do checks.md; as 4 saídas têm um caso em C86 e a fraqueza do caso `missing` está registrada em Checks e Coverage, não nesta linha | yes |
| Decides, not reached across a boundary - carried from 89f3d3a | `packages/core/src/registry.ts` | C30 `registry.test.ts:41,:47,:64,:29`; C31 `registry-bin.test.ts:25-28` | yes |
| Decides, not reached across a boundary - carried from 89f3d3a | `packages/cli/src/commands/doctor.ts` | C56 `doctor.test.ts:18,:30` | yes |
| Decides, not reached across a boundary - carried from 89f3d3a | `tools/stale.ts` | C78 `stale.test.ts:18-21,:28-29`; guard `reviewed` não parseável (`stale.ts:26`) é observação | yes |
| Decides, not reached across a boundary - carried from 89f3d3a | `tools/allowlist.ts` | C75 `allowlist.test.ts:32-34,:52-53,:56-57,:61-62` | yes |
| Entry point that decides nothing - carried from 89f3d3a | `apps/site/src/**` | na fronteira: C61-C71; ordenação C63 `build.test.ts:64` (F4), ramo deprecada C67, busca sem resultado C64 | yes |
| Entry point that decides nothing - carried from 89f3d3a | `packages/core/src/bin/validate.ts`, `bin/registry.ts` | C16 `validate-bin.test.ts:18,:22`, C31, C60 `mirrors.test.ts:24-37` | yes |
| Instrumentation, pass-throughs - carried from 89f3d3a | `packages/cli/src/download.ts` | coberto pelo consumidor: C58 `install.test.ts:192-193,:201-203,:211-213`, C47 `:97`, C82 | yes |

## Faults injected

verified at 75e4b94 para Fe-Fh (a superfície que o fix tocou: `packages/core/src/frontmatter.ts`, agora coberto pelo `it.each` de `rules.test.ts:23-35`); Fa-Fd `carried from 123d165` e F1-F4 `carried from 89f3d3a` (o fix não tocou `hash.ts`, `installer.ts`, `update.ts`, `catalog.ts`, `agents.ts`, `new-skill.ts` nem `download.ts`, nem as provas que os mataram - `git diff 123d165..75e4b94 --stat` lista só `rules.test.ts` e `.specs/`). Isolamento: `git worktree add <scratch>/verify-wt3 HEAD` (HEAD `75e4b94`), `pnpm install --frozen-lockfile --offline` exit 0 (duas sessões: o worktree foi criado para Fe-Fg, removido, e recriado para Fh); nunca `git stash`. Porcelain da árvore real: vazio antes e vazio depois de cada sessão; worktree removido com `git worktree remove --force` + `git worktree prune` (`git worktree list` só com a árvore real; diretório ausente); nenhum `.astro-build.lock` deixado. Cada falha foi aplicada sozinha (1 arquivo, `git diff --stat` = 1 file changed), a prova mais estreita rodada no scratch com `--reporter=verbose` e o arquivo restaurado com `git checkout --` antes da seguinte (`git status --porcelain` do scratch com 0 linhas após cada uma); o worktree sem mutação passa os 4 testes do `describe` (sanidade).

| Mutation | Location | Killed |
| --- | --- | --- |
| F1 - algoritmo do `contentHash`: separadores removidos - carried from 89f3d3a | `packages/core/src/hash.ts:26` | yes |
| F2 - comparação do `contentHash` no install desligada - carried from 89f3d3a | `packages/cli/src/installer.ts:50` | yes |
| F3 - decisão de três vias do `update` invertida (`!==` → `===`) - carried from 89f3d3a | `packages/cli/src/commands/update.ts:37` | yes |
| F4 - ordenação da home dentro da categoria invertida - carried from 89f3d3a | `apps/site/src/lib/catalog.ts:27` | yes |
| Fa (= F5 re-injetada) - tabela da door 10: path global de `cursor` `.cursor/skills` → `.cursorx/skills` - carried from 123d165 | `packages/cli/src/agents.ts:16` | yes |
| Fb - `update`: estado `missing` descrito como `up to date` - carried from 123d165 | `packages/cli/src/commands/update.ts:45` | yes |
| Fc - `new-skill`: guard `skills/<name>/ already exists` removido (linhas 17-20 apagadas; scaffold sobrescreve) - carried from 123d165 | `tools/new-skill.ts:17-20` | yes |
| Fd - `download`: 404 tratado como sucesso (`if (!res.ok)` → `if (!res.ok && res.status !== 404)`) - carried from 123d165 | `packages/cli/src/download.ts:32` | yes |
| Fe - parser: frontmatter não terminado reportado como `missing frontmatter` (`"unterminated frontmatter"` → `"missing frontmatter"`) | `packages/core/src/frontmatter.ts:27` | yes |
| Ff - parser: YAML inválido engolido como mapa vazio (`catch` passa a `parsed = {}` em vez de retornar `frontmatterError`) | `packages/core/src/frontmatter.ts:33-35` | yes |
| Fg - parser: frontmatter não terminado não reportado (fence final assumido no EOF: `end < 0 ? lines.length : end`) | `packages/core/src/frontmatter.ts:27` | yes |
| Fh - parser: frontmatter ausente reportado como `unterminated frontmatter` (`"missing frontmatter"` → `"unterminated frontmatter"`) | `packages/core/src/frontmatter.ts:25` | no |

Provas rodadas por falha (todas no scratch, `pnpm vitest run packages/core/test/rules.test.ts -t "frontmatter parse error" --reporter=verbose`):

- Fe → exit 1, `× reports frontmatter/parse for unterminated frontmatter` (`AssertionError: expected 'missing frontmatter' to contain 'unterminated frontmatter'`, `rules.test.ts:34`); os outros 3 testes do `describe` verdes (`1 failed | 3 passed`).
- Ff → exit 1, `× reports frontmatter/parse for invalid YAML` (`AssertionError: expected [] to have a length of 1 but got +0`, `rules.test.ts:33` - com `parsed = {}` o `frontmatter` fica `{}`, truthy, e `frontmatterParse` devolve `[]`).
- Fg → exit 1, `× reports frontmatter/parse for unterminated frontmatter` (`expected [] to have a length of 1 but got +0`, `rules.test.ts:33` - o YAML até o EOF é um mapa válido e nenhum achado é emitido).
- Fh → exit 0, os 4 testes verdes (`4 passed | 60 skipped`). Segunda leitura, `pnpm vitest run packages/core/test test/repo` no scratch com o mutante → exit 0, **106 passed, 0 failed** (17 arquivos). `rg -n 'missing frontmatter'` em `packages/`, `test/`, `apps/` (sem `node_modules`) → só `frontmatter.ts:25`. Nenhuma prova da árvore distingue a causa `missing` de qualquer outra: `rules.test.ts:19` assere só `toContain("frontmatter/parse")`. **Mutante sobrevivente.**

Fe e Fh são a mesma classe de falta (confusão de causa) nas duas direções: `unterminated → missing` morre pela asserção `:34` que o fix acrescentou; `missing → unterminated` sobrevive porque o caso `missing` ficou fora do `it.each`, na asserção pré-fix. Um mutante que emitisse dois achados para `missing` sobreviveria pela mesma razão (sem `toHaveLength(1)`); não foi rodado porque cai na mesma superfície de asserção.

## Swept existing rows

carried from 89f3d3a - o `## Swept` do checks.md não tem linha resolvida para `existing` e nem o fix da rodada 2 nem o desta rodada o alteraram (`git diff 123d165..75e4b94 -- checks.md` toca só o claim de C86 e a linha nova de Coverage). A linha de `concurrency` continua exata contra `lockfile.ts:34-41`.

## Precision gaps and observations (not FAIL)

- **Prosa do `## Test policy` do checks.md ainda diz "16 rule ids" (linha de evidência de `packages/core/src/rules/*.ts`) e "as 16 regras" (linha de custo)** enquanto a linha de Coverage e o código dizem 17 (`frontmatter/parse`, acrescentado após a rodada 1). Contagem desatualizada em prosa; tabela e código concordam.
- **O fallback `?? "invalid frontmatter"` em `rules/frontmatter.ts:27` é inalcançável**: os 4 retornos de `parseSkill` sem `frontmatter` (`frontmatter.ts:25,27,34,37`) carregam `frontmatterError`. Não é uma quinta causa; a linha `frontmatter parse causes (4)` está completa.
- O comentário em `rules.test.ts:22` ("C86 names three; the fourth is the parser's own") descreve o claim antigo; o claim reescrito nomeia as 4. Só comentário.
- carried from 123d165: linha `update outcomes (6)` do checks.md conta `--force` como membro e omite `deprecated`; recomputada de `update.ts:14-20`, os 6 kinds têm prova. Gap de precisão da tabela, não de código.
- carried from 89f3d3a: AC 61 nomeia `pnpm build`, C61 roda `astro build` direto; C76/C77 asserem texto do `github-script`; 404 do site provado como existência de `dist/404.html`; guards `stale.ts:26` e `allowlist.ts:30` sem caso; `install` sem `-a` usa `auto` (C49 `:126`).

## Gate

Comandos rodados na raiz do repositório real em `75e4b94`, todos com exit 0:

- `pnpm install --frozen-lockfile` - exit 0, `Already up to date` (C1)
- lote 1: `pnpm vitest run test/repo/{workspace,new-skill,docs,github,gitignore,catalog,marketplace,workflows,allowlist,stale,release}.test.ts packages/core/test/{validate,validate-bin,rules,registry,registry-bin,exports}.test.ts -t "<alternação dos 55 nomes de C1-C39, C72-C81, C85, C86>" --reporter=verbose` - 17 arquivos, **104 passed, 0 failed** (2 skipped fora do filtro). Nota de método: a primeira invocação deste lote omitiu `evals shape` da alternação e deixou os 3 testes de C29 como `↓ skipped` (101 passed); o lote foi refeito inteiro com o nome incluído e é essa segunda saída que conta aqui e na coluna `Proof run`.
- lote 2: `pnpm vitest run packages/cli/test/{list,install,lockfile,update,doctor,remove,exit-codes,mirrors,download,agents}.test.ts -t "<alternação dos 24 nomes de C43-C60, C82, C83, C84>" --reporter=verbose` - 10 arquivos, **25 passed, 0 failed** (4 skipped fora do filtro)
- lote 3: `pnpm vitest run apps/site/test/{build,search,deprecated}.test.ts -t "<alternação dos 11 nomes de C61-C71>" --reporter=verbose` - 3 arquivos, **11 passed, 0 failed** (astro build real; nenhum `.astro-build.lock` restante)
- `pnpm validate` - exit 0, `validated 5 skill(s): 0 error(s), 0 warning(s)` (C15)
- `bash tools/spike/npx-skills-list.sh` - exit 0, `PASS: npx skills lists exactly: mass-code-review mass-commit-message mass-pr-description mass-security-checklist mass-skill-authoring` (C40) · `bash tools/spike/claude-plugin-validate.sh` - exit 0, `✔ Validation passed` (C41) · `bash tools/spike/claude-plugin-install.sh` - exit 0, `installPath: ~/.claude/plugins/cache/mass-solutions/mass-solutions-skills/0.1.0`, `PASS: plugin installs exactly: ...`, desinstalado ao final (`claude plugin list --json` sem `mass-solutions-skills` depois) (C42)
- Fault injection no scratch: 4 mutantes novos (Fe-Fh), 3 mortos, **1 sobrevivente (Fh)**; Fa-Fd carried from 123d165 (4 mortos); F1-F4 carried from 89f3d3a (4 mortos)

`python3 .claude/skills/tlc-spec-lean/scripts/validate_verification.py mass-solutions-skills` - saída registrada em chat após a escrita deste relatório.

**Ranked gaps**:

1. C86 - claim "cada uma das 4 causas gera exatamente um `frontmatter/parse` cuja mensagem nomeia a causa" provado nessa força para 3 causas; a causa `missing frontmatter` (`packages/core/src/frontmatter.ts:25`) é asserida por `packages/core/test/rules.test.ts:19` só com `expect(rules(f)).toContain("frontmatter/parse")` - sem `toHaveLength(1)` e sem `message` - e o mutante Fh (`missing` rotulado `unterminated frontmatter`) sobrevive aos 4 testes de C86 e à suíte inteira do core + repo (106 passed). Correção de uma linha: acrescentar `["missing frontmatter", "no frontmatter\n"]` ao `it.each` de `rules.test.ts:23-27` (o caso `:17-20` pode ser removido ou mantido), ou acrescentar em `:17-20` `expect(hit).toHaveLength(1)` e `expect(hit[0].message).toContain("missing frontmatter")`. Reflexo em Coverage `frontmatter parse causes (4)`, membro `missing frontmatter`.
